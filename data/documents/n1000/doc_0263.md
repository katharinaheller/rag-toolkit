        # Ablation Studies

        DOCUMENT_ID: doc_n1000_0263
        CATEGORY: Evaluation
        TAGS: ablation studies, GPU memory

        ## Overview

This document covers ablation studies in the context of local RAG systems. The approach processes GPU memory using modular techniques suitable for CPU-only environments.

IndexingOrchestrator indexes the core logic, ensuring configurable behavior across pipeline runs.

## Retrieval Pipeline

The ablation studies retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-68 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

GPU memory ensures no text or metadata enters the index backends directly.

## Architecture

The ablation studies subsystem is organized around three layers:

- **Ingestion**: streaming-safe document loading and chunking
- **Embedding**: configurable vector generation via BGE-M3
- **Retrieval**: deterministic hybrid search with score fusion

IngestionPipeline acts as the central coordinator, delegating GPU memory to downstream modules.
