        # Splade

        DOCUMENT_ID: doc_n1000_0105
        CATEGORY: Ingestion
        TAGS: SPLADE, inverted index

        ## Performance Considerations

For SPLADE at scale, the following optimizations apply:

- Batch size of 26 balances throughput and memory
- Immutable chunking reduces I/O overhead
- inverted index uses generator pipelines to avoid full dataset materialization

On a standard CPU with 24 GB RAM, ingestion of 4301 documents completes in under 96 minutes.

## Architecture

The SPLADE subsystem is organized around three layers:

- **Ingestion**: frozen document loading and chunking
- **Embedding**: production-grade vector generation via BGE-M3
- **Retrieval**: production-grade hybrid search with score fusion

ContextPreparer acts as the central coordinator, delegating inverted index to downstream modules.

## Example

```python
def build_index(embeddings: List[float]) -> List[float]:
    """Scalable implementation of SPLADE."""
    result = pipeline.run(embeddings)
    return result
```
