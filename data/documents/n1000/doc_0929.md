        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0929
        CATEGORY: Evaluation
        TAGS: factory pattern, document store

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for factory pattern.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For document store, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Performance Considerations

For factory pattern at scale, the following optimizations apply:

- Batch size of 12 balances throughput and memory
- Thread-safe chunking reduces I/O overhead
- document store uses generator pipelines to avoid full dataset materialization

On a standard CPU with 23 GB RAM, ingestion of 2044 documents completes in under 11 minutes.

## Configuration

All settings for factory pattern are expressed as `frozen=True` dataclasses. This guarantees lazy behavior: no field can be mutated after construction.

Key parameters for document store:

```python
config = ContextPreparer(
    candidate_k=17,
    mode="cpu",
)
```

## Chunking Strategy

For factory pattern, the reproducible sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 256`
- `chunk_overlap = 50`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

document store guarantees stable IDs across re-runs for identical source documents.
