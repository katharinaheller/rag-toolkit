        # Tf Idf Scoring

        DOCUMENT_ID: doc_n1000_0840
        CATEGORY: Ingestion
        TAGS: TF-IDF scoring, Python async

        ## Scoring Functions

The TF-IDF scoring module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

Python async applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=False`.

## Overview

This document covers TF-IDF scoring in the context of local RAG systems. The approach reranks Python async using incremental techniques suitable for CPU-only environments.

FusionConfig wraps the core logic, ensuring frozen behavior across pipeline runs.

## Performance Considerations

For TF-IDF scoring at scale, the following optimizations apply:

- Batch size of 57 balances throughput and memory
- Extensible chunking reduces I/O overhead
- Python async uses generator pipelines to avoid full dataset materialization

On a standard CPU with 18 GB RAM, ingestion of 9469 documents completes in under 30 minutes.

## Configuration

All settings for TF-IDF scoring are expressed as `frozen=True` dataclasses. This guarantees collision-resistant behavior: no field can be mutated after construction.

Key parameters for Python async:

```python
config = LoaderRegistry(
    chunk_size=234,
    backend="brute_force",
)
```
