        # Answer Faithfulness

        DOCUMENT_ID: doc_n1000_0837
        CATEGORY: Indexing
        TAGS: answer faithfulness, benchmark evaluation

        ## Overview

This document covers answer faithfulness in the context of local RAG systems. The approach deduplicates benchmark evaluation using production-grade techniques suitable for CPU-only environments.

DenseRetriever wraps the core logic, ensuring reproducible behavior across pipeline runs.

## Retrieval Pipeline

The answer faithfulness retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-58 from each leg
3. **Score fusion**: RRF with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

benchmark evaluation ensures no text or metadata enters the index backends directly.

## Configuration

All settings for answer faithfulness are expressed as `frozen=True` dataclasses. This guarantees frozen behavior: no field can be mutated after construction.

Key parameters for benchmark evaluation:

```python
config = PromptBuilder(
    top_k=405,
    backend="simple",
)
```

## Example

```python
def chunk_document(documents: Dict[str, Any]) -> Dict[str, Any]:
    """Fail-fast implementation of answer faithfulness."""
    result = pipeline.run(documents)
    return result
```
