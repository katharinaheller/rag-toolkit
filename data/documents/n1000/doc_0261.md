        # Cpu Inference

        DOCUMENT_ID: doc_n1000_0261
        CATEGORY: Infrastructure
        TAGS: CPU inference, sliding window chunking

        ## Implementation Notes

CPU inference relies on incremental SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The sliding window chunking component uses a typed inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Chunking Strategy

For CPU inference, the streaming-safe sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 1024`
- `chunk_overlap = 50`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

sliding window chunking guarantees stable IDs across re-runs for identical source documents.

## Performance Considerations

For CPU inference at scale, the following optimizations apply:

- Batch size of 58 balances throughput and memory
- Efficient chunking reduces I/O overhead
- sliding window chunking uses generator pipelines to avoid full dataset materialization

On a standard CPU with 9 GB RAM, ingestion of 2338 documents completes in under 38 minutes.

## Configuration

All settings for CPU inference are expressed as `frozen=True` dataclasses. This guarantees scalable behavior: no field can be mutated after construction.

Key parameters for sliding window chunking:

```python
config = BaselineReranker(
    top_k=320,
    tokenizer="brute_force",
)
```
