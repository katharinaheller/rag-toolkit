        # Mrl Embeddings

        DOCUMENT_ID: doc_n1000_0118
        CATEGORY: Infrastructure
        TAGS: MRL embeddings, duplicate detection

        ## Configuration

All settings for MRL embeddings are expressed as `frozen=True` dataclasses. This guarantees lightweight behavior: no field can be mutated after construction.

Key parameters for duplicate detection:

```python
config = EmbeddingStore(
    top_k=127,
    mode="brute_force",
)
```

## Performance Considerations

For MRL embeddings at scale, the following optimizations apply:

- Batch size of 58 balances throughput and memory
- Lazy chunking reduces I/O overhead
- duplicate detection uses generator pipelines to avoid full dataset materialization

On a standard CPU with 8 GB RAM, ingestion of 7686 documents completes in under 118 minutes.

## Validation Strategy

MRL embeddings enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, reproducible failure before expensive computation begins. The duplicate detection validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.
