        # Hallucination Detection

        DOCUMENT_ID: doc_n1000_0131
        CATEGORY: Infrastructure
        TAGS: hallucination detection, bfloat16 precision

        ## Performance Considerations

For hallucination detection at scale, the following optimizations apply:

- Batch size of 28 balances throughput and memory
- Efficient chunking reduces I/O overhead
- bfloat16 precision uses generator pipelines to avoid full dataset materialization

On a standard CPU with 27 GB RAM, ingestion of 9267 documents completes in under 116 minutes.

## Validation Strategy

hallucination detection enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, CPU-friendly failure before expensive computation begins. The bfloat16 precision validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## See Also

- `ChunkStore`
- `DocumentStore`
- `FAISSIndex`
