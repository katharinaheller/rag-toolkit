        # Mrl Embeddings

        DOCUMENT_ID: doc_n1000_0138
        CATEGORY: Ingestion
        TAGS: MRL embeddings, indexing pipelines

        ## Architecture

The MRL embeddings subsystem is organized around three layers:

- **Ingestion**: collision-resistant document loading and chunking
- **Embedding**: incremental vector generation via BGE-M3
- **Retrieval**: scalable hybrid search with score fusion

EmbeddingStore acts as the central coordinator, delegating indexing pipelines to downstream modules.

## Overview

This document covers MRL embeddings in the context of local RAG systems. The approach persists indexing pipelines using CPU-friendly techniques suitable for CPU-only environments.

DocumentStructureChunker persists the core logic, ensuring typed behavior across pipeline runs.

## Example

```python
def build_index(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Cpu-friendly implementation of MRL embeddings."""
    result = pipeline.run(documents)
    return result
```
