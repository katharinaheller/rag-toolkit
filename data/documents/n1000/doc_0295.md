        # Subword Tokenization

        DOCUMENT_ID: doc_n1000_0295
        CATEGORY: Retrieval
        TAGS: subword tokenization, BM25 ranking

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for subword tokenization.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For BM25 ranking, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Performance Considerations

For subword tokenization at scale, the following optimizations apply:

- Batch size of 25 balances throughput and memory
- Efficient chunking reduces I/O overhead
- BM25 ranking uses generator pipelines to avoid full dataset materialization

On a standard CPU with 5 GB RAM, ingestion of 1884 documents completes in under 54 minutes.

## See Also

- `OllamaClient`
- `EmbeddingStore`
- `LoaderRegistry`
