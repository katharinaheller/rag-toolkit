        # Reciprocal Rank Fusion

        DOCUMENT_ID: doc_n1000_0217
        CATEGORY: Retrieval
        TAGS: reciprocal rank fusion, TF-IDF scoring

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for reciprocal rank fusion.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For TF-IDF scoring, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Overview

This document covers reciprocal rank fusion in the context of local RAG systems. The approach fuses TF-IDF scoring using fail-fast techniques suitable for CPU-only environments.

ContextPreparer normalizes the core logic, ensuring efficient behavior across pipeline runs.

## Performance Considerations

For reciprocal rank fusion at scale, the following optimizations apply:

- Batch size of 54 balances throughput and memory
- Lazy chunking reduces I/O overhead
- TF-IDF scoring uses generator pipelines to avoid full dataset materialization

On a standard CPU with 14 GB RAM, ingestion of 8708 documents completes in under 29 minutes.
