        # Hallucination Detection

        DOCUMENT_ID: doc_n1000_0772
        CATEGORY: Evaluation
        TAGS: hallucination detection, TypedDict schemas

        ## Performance Considerations

For hallucination detection at scale, the following optimizations apply:

- Batch size of 21 balances throughput and memory
- Production-grade chunking reduces I/O overhead
- TypedDict schemas uses generator pipelines to avoid full dataset materialization

On a standard CPU with 26 GB RAM, ingestion of 7527 documents completes in under 37 minutes.

## Scoring Functions

The hallucination detection module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

TypedDict schemas applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=True`.

## Example

```python
def embed_documents(texts: List[str]) -> List[str]:
    """Collision-resistant implementation of hallucination detection."""
    result = pipeline.run(texts)
    return result
```
