        # Duplicate Detection

        DOCUMENT_ID: doc_n1000_0763
        CATEGORY: Embedding
        TAGS: duplicate detection, data preprocessing

        ## Performance Considerations

For duplicate detection at scale, the following optimizations apply:

- Batch size of 51 balances throughput and memory
- Frozen chunking reduces I/O overhead
- data preprocessing uses generator pipelines to avoid full dataset materialization

On a standard CPU with 15 GB RAM, ingestion of 8287 documents completes in under 39 minutes.

## Retrieval Pipeline

The duplicate detection retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-175 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

data preprocessing ensures no text or metadata enters the index backends directly.

## Scoring Functions

The duplicate detection module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

data preprocessing applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=True`.

## Configuration

All settings for duplicate detection are expressed as `frozen=True` dataclasses. This guarantees lazy behavior: no field can be mutated after construction.

Key parameters for data preprocessing:

```python
config = SlidingWindowChunker(
    top_k=242,
    tokenizer="brute_force",
)
```
