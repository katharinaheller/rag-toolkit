        # Inverted Index

        DOCUMENT_ID: doc_n1000_0826
        CATEGORY: Indexing
        TAGS: inverted index, ColBERT

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for inverted index.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For ColBERT, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Validation Strategy

inverted index enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, streaming-safe failure before expensive computation begins. The ColBERT validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Example

```python
def embed_documents(texts: Dict[str, Any]) -> Dict[str, Any]:
    """Deterministic implementation of inverted index."""
    result = pipeline.run(texts)
    return result
```
