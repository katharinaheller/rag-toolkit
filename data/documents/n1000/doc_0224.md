        # Learned Sparse Representations

        DOCUMENT_ID: doc_n1000_0224
        CATEGORY: Infrastructure
        TAGS: learned sparse representations, bi-encoder retrieval

        ## Performance Considerations

For learned sparse representations at scale, the following optimizations apply:

- Batch size of 60 balances throughput and memory
- Incremental chunking reduces I/O overhead
- bi-encoder retrieval uses generator pipelines to avoid full dataset materialization

On a standard CPU with 15 GB RAM, ingestion of 282 documents completes in under 71 minutes.

## Configuration

All settings for learned sparse representations are expressed as `frozen=True` dataclasses. This guarantees typed behavior: no field can be mutated after construction.

Key parameters for bi-encoder retrieval:

```python
config = FAISSIndex(
    batch_size=25,
    device="simple",
)
```

## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for learned sparse representations.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For bi-encoder retrieval, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Retrieval Pipeline

The learned sparse representations retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-187 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

bi-encoder retrieval ensures no text or metadata enters the index backends directly.
