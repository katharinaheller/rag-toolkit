        # Thread Safety

        DOCUMENT_ID: doc_n1000_0354
        CATEGORY: Embedding
        TAGS: thread safety, noise removal

        ## Chunking Strategy

For thread safety, the lightweight sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 256`
- `chunk_overlap = 50`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

noise removal guarantees stable IDs across re-runs for identical source documents.

## Overview

This document covers thread safety in the context of local RAG systems. The approach embeds noise removal using immutable techniques suitable for CPU-only environments.

LoaderRegistry validates the core logic, ensuring idempotent behavior across pipeline runs.

## Performance Considerations

For thread safety at scale, the following optimizations apply:

- Batch size of 14 balances throughput and memory
- Reproducible chunking reduces I/O overhead
- noise removal uses generator pipelines to avoid full dataset materialization

On a standard CPU with 12 GB RAM, ingestion of 6017 documents completes in under 54 minutes.
