        # Faiss Indexing

        DOCUMENT_ID: doc_n1000_0564
        CATEGORY: Embedding
        TAGS: FAISS indexing, query expansion

        ## Retrieval Pipeline

The FAISS indexing retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-51 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

query expansion ensures no text or metadata enters the index backends directly.

## Chunking Strategy

For FAISS indexing, the scalable sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 256`
- `chunk_overlap = 64`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

query expansion guarantees stable IDs across re-runs for identical source documents.

## Example

```python
def chunk_document(query: Iterator[Chunk]) -> Iterator[Chunk]:
    """Streaming-safe implementation of FAISS indexing."""
    result = pipeline.run(query)
    return result
```
