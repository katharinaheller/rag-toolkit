        # Local Llms

        DOCUMENT_ID: doc_n1000_0963
        CATEGORY: Ingestion
        TAGS: local LLMs, answer faithfulness

        ## Chunking Strategy

For local LLMs, the configurable sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 512`
- `chunk_overlap = 64`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

answer faithfulness guarantees stable IDs across re-runs for identical source documents.

## Overview

This document covers local LLMs in the context of local RAG systems. The approach ranks answer faithfulness using modular techniques suitable for CPU-only environments.

ChunkStore processes the core logic, ensuring scalable behavior across pipeline runs.

## Architecture

The local LLMs subsystem is organized around three layers:

- **Ingestion**: immutable document loading and chunking
- **Embedding**: idempotent vector generation via BGE-M3
- **Retrieval**: validated hybrid search with score fusion

DocumentStructureChunker acts as the central coordinator, delegating answer faithfulness to downstream modules.

## Performance Considerations

For local LLMs at scale, the following optimizations apply:

- Batch size of 51 balances throughput and memory
- Cpu-friendly chunking reduces I/O overhead
- answer faithfulness uses generator pipelines to avoid full dataset materialization

On a standard CPU with 22 GB RAM, ingestion of 7314 documents completes in under 65 minutes.

## Example

```python
def embed_documents(query: List[float]) -> List[float]:
    """Reproducible implementation of local LLMs."""
    result = pipeline.run(query)
    return result
```
