        # Bi Encoder Retrieval

        DOCUMENT_ID: doc_n1000_0379
        CATEGORY: Infrastructure
        TAGS: bi-encoder retrieval, approximate nearest neighbors

        ## Configuration

All settings for bi-encoder retrieval are expressed as `frozen=True` dataclasses. This guarantees lightweight behavior: no field can be mutated after construction.

Key parameters for approximate nearest neighbors:

```python
config = HybridRetriever(
    candidate_k=229,
    tokenizer="brute_force",
)
```

## Architecture

The bi-encoder retrieval subsystem is organized around three layers:

- **Ingestion**: fail-fast document loading and chunking
- **Embedding**: reproducible vector generation via BGE-M3
- **Retrieval**: frozen hybrid search with score fusion

ContextPreparer acts as the central coordinator, delegating approximate nearest neighbors to downstream modules.

## Example

```python
def embed_documents(embeddings: Iterator[Chunk]) -> Iterator[Chunk]:
    """Frozen implementation of bi-encoder retrieval."""
    result = pipeline.run(embeddings)
    return result
```
