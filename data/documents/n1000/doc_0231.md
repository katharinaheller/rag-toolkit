        # Python Async

        DOCUMENT_ID: doc_n1000_0231
        CATEGORY: RAG Systems
        TAGS: Python async, TF-IDF scoring

        ## Retrieval Pipeline

The Python async retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-117 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

TF-IDF scoring ensures no text or metadata enters the index backends directly.

## Architecture

The Python async subsystem is organized around three layers:

- **Ingestion**: idempotent document loading and chunking
- **Embedding**: fail-fast vector generation via BGE-M3
- **Retrieval**: incremental hybrid search with score fusion

RetrievalOrchestrator acts as the central coordinator, delegating TF-IDF scoring to downstream modules.

## Performance Considerations

For Python async at scale, the following optimizations apply:

- Batch size of 37 balances throughput and memory
- Streaming-safe chunking reduces I/O overhead
- TF-IDF scoring uses generator pipelines to avoid full dataset materialization

On a standard CPU with 16 GB RAM, ingestion of 3751 documents completes in under 67 minutes.

## Validation Strategy

Python async enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, configurable failure before expensive computation begins. The TF-IDF scoring validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Example

```python
def fuse_results(embeddings: Iterator[Chunk]) -> Iterator[Chunk]:
    """Frozen implementation of Python async."""
    result = pipeline.run(embeddings)
    return result
```
