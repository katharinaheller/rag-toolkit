        # Chain Of Thought Prompting

        DOCUMENT_ID: doc_n1000_0328
        CATEGORY: Infrastructure
        TAGS: chain-of-thought prompting, benchmark evaluation

        ## Retrieval Pipeline

The chain-of-thought prompting retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-71 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

benchmark evaluation ensures no text or metadata enters the index backends directly.

## Scoring Functions

The chain-of-thought prompting module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

benchmark evaluation applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=True`.

## Example

```python
def build_index(chunks: Iterator[Chunk]) -> Iterator[Chunk]:
    """Configurable implementation of chain-of-thought prompting."""
    result = pipeline.run(chunks)
    return result
```

## See Also

- `ModelCache`
- `SparseIndex`
- `SlidingWindowChunker`
- `ContextPreparer`
