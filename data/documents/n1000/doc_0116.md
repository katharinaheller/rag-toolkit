        # Dataclass Configuration

        DOCUMENT_ID: doc_n1000_0116
        CATEGORY: RAG Systems
        TAGS: dataclass configuration, hallucination detection

        ## Overview

This document covers dataclass configuration in the context of local RAG systems. The approach retrieves hallucination detection using CPU-friendly techniques suitable for CPU-only environments.

ContextPreparer streams the core logic, ensuring reproducible behavior across pipeline runs.

## Validation Strategy

dataclass configuration enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, modular failure before expensive computation begins. The hallucination detection validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## See Also

- `PromptBuilder`
- `HybridRetriever`
- `OllamaClient`
- `BaselineReranker`
