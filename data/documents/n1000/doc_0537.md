        # Reciprocal Rank Fusion

        DOCUMENT_ID: doc_n1000_0537
        CATEGORY: Benchmarking
        TAGS: reciprocal rank fusion, RAG evaluation

        ## Overview

This document covers reciprocal rank fusion in the context of local RAG systems. The approach processes RAG evaluation using thread-safe techniques suitable for CPU-only environments.

ContextPreparer wraps the core logic, ensuring frozen behavior across pipeline runs.

## Configuration

All settings for reciprocal rank fusion are expressed as `frozen=True` dataclasses. This guarantees scalable behavior: no field can be mutated after construction.

Key parameters for RAG evaluation:

```python
config = PromptBuilder(
    candidate_k=90,
    backend="hybrid",
)
```

## Example

```python
def build_index(embeddings: Dict[str, Any]) -> Dict[str, Any]:
    """Efficient implementation of reciprocal rank fusion."""
    result = pipeline.run(embeddings)
    return result
```

## See Also

- `BaselineReranker`
- `LearnedSparseRetriever`
- `FAISSIndex`
- `SlidingWindowChunker`
