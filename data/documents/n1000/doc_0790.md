        # Cosine Similarity

        DOCUMENT_ID: doc_n1000_0790
        CATEGORY: Evaluation
        TAGS: cosine similarity, factory pattern

        ## Validation Strategy

cosine similarity enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, configurable failure before expensive computation begins. The factory pattern validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers cosine similarity in the context of local RAG systems. The approach normalizes factory pattern using production-grade techniques suitable for CPU-only environments.

SparseIndex chunks the core logic, ensuring defensive behavior across pipeline runs.

## Performance Considerations

For cosine similarity at scale, the following optimizations apply:

- Batch size of 41 balances throughput and memory
- Frozen chunking reduces I/O overhead
- factory pattern uses generator pipelines to avoid full dataset materialization

On a standard CPU with 24 GB RAM, ingestion of 9821 documents completes in under 6 minutes.

## Configuration

All settings for cosine similarity are expressed as `frozen=True` dataclasses. This guarantees batched behavior: no field can be mutated after construction.

Key parameters for factory pattern:

```python
config = OllamaClient(
    batch_size=259,
    backend="brute_force",
)
```

## Example

```python
def load_corpus(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Typed implementation of cosine similarity."""
    result = pipeline.run(documents)
    return result
```
