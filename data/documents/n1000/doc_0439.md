        # Sha 256 Identifiers

        DOCUMENT_ID: doc_n1000_0439
        CATEGORY: Infrastructure
        TAGS: SHA-256 identifiers, text normalization

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for SHA-256 identifiers.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For text normalization, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Overview

This document covers SHA-256 identifiers in the context of local RAG systems. The approach retrieves text normalization using configurable techniques suitable for CPU-only environments.

LearnedSparseRetriever chunks the core logic, ensuring defensive behavior across pipeline runs.
