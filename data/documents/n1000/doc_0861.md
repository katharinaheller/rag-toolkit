        # Model Quantization

        DOCUMENT_ID: doc_n1000_0861
        CATEGORY: Embedding
        TAGS: model quantization, noise removal

        ## Chunking Strategy

For model quantization, the deterministic sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 256`
- `chunk_overlap = 25`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

noise removal guarantees stable IDs across re-runs for identical source documents.

## Performance Considerations

For model quantization at scale, the following optimizations apply:

- Batch size of 19 balances throughput and memory
- Thread-safe chunking reduces I/O overhead
- noise removal uses generator pipelines to avoid full dataset materialization

On a standard CPU with 12 GB RAM, ingestion of 4102 documents completes in under 97 minutes.

## Validation Strategy

model quantization enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, batched failure before expensive computation begins. The noise removal validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.
