        # Cross Encoder Reranking

        DOCUMENT_ID: doc_n1000_0567
        CATEGORY: Embedding
        TAGS: cross-encoder reranking, zero-shot inference

        ## Scoring Functions

The cross-encoder reranking module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

zero-shot inference applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=True`.

## Overview

This document covers cross-encoder reranking in the context of local RAG systems. The approach builds zero-shot inference using production-grade techniques suitable for CPU-only environments.

LearnedSparseRetriever deduplicates the core logic, ensuring production-grade behavior across pipeline runs.
