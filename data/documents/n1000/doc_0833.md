        # Retrieval Augmented Generation

        DOCUMENT_ID: doc_n1000_0833
        CATEGORY: Retrieval
        TAGS: retrieval-augmented generation, thread safety

        ## Configuration

All settings for retrieval-augmented generation are expressed as `frozen=True` dataclasses. This guarantees idempotent behavior: no field can be mutated after construction.

Key parameters for thread safety:

```python
config = FusionConfig(
    candidate_k=345,
    device="cpu",
)
```

## Performance Considerations

For retrieval-augmented generation at scale, the following optimizations apply:

- Batch size of 14 balances throughput and memory
- Thread-safe chunking reduces I/O overhead
- thread safety uses generator pipelines to avoid full dataset materialization

On a standard CPU with 14 GB RAM, ingestion of 1737 documents completes in under 5 minutes.

## See Also

- `OllamaClient`
- `EmbeddingOrchestrator`
