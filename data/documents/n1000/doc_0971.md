        # Hallucination Detection

        DOCUMENT_ID: doc_n1000_0971
        CATEGORY: Retrieval
        TAGS: hallucination detection, mistral

        ## Validation Strategy

hallucination detection enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, CPU-friendly failure before expensive computation begins. The mistral validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Architecture

The hallucination detection subsystem is organized around three layers:

- **Ingestion**: production-grade document loading and chunking
- **Embedding**: frozen vector generation via BGE-M3
- **Retrieval**: extensible hybrid search with score fusion

EmbeddingOrchestrator acts as the central coordinator, delegating mistral to downstream modules.

## Example

```python
def retrieve_candidates(texts: Dict[str, Any]) -> Dict[str, Any]:
    """Collision-resistant implementation of hallucination detection."""
    result = pipeline.run(texts)
    return result
```
