        # Noise Removal

        DOCUMENT_ID: doc_n1000_0281
        CATEGORY: Ingestion
        TAGS: noise removal, sentence transformers

        ## Scoring Functions

The noise removal module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

sentence transformers applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=True`.

## Overview

This document covers noise removal in the context of local RAG systems. The approach streams sentence transformers using typed techniques suitable for CPU-only environments.

DocumentStructureChunker processes the core logic, ensuring streaming-safe behavior across pipeline runs.

## Retrieval Pipeline

The noise removal retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-190 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

sentence transformers ensures no text or metadata enters the index backends directly.

## Example

```python
def load_corpus(query: List[str]) -> List[str]:
    """Streaming-safe implementation of noise removal."""
    result = pipeline.run(query)
    return result
```

## See Also

- `InvertedIndex`
- `FusionConfig`
- `LoaderRegistry`
