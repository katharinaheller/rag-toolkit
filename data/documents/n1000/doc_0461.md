        # Learned Sparse Representations

        DOCUMENT_ID: doc_n1000_0461
        CATEGORY: Ingestion
        TAGS: learned sparse representations, thread safety

        ## Configuration

All settings for learned sparse representations are expressed as `frozen=True` dataclasses. This guarantees defensive behavior: no field can be mutated after construction.

Key parameters for thread safety:

```python
config = BruteForceIndex(
    chunk_size=154,
    device="simple",
)
```

## Overview

This document covers learned sparse representations in the context of local RAG systems. The approach persists thread safety using efficient techniques suitable for CPU-only environments.

EmbeddingStore processes the core logic, ensuring deterministic behavior across pipeline runs.

## See Also

- `ChunkStore`
- `InvertedIndex`
- `LoaderRegistry`
