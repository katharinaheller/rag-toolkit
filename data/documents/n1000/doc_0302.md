        # Vocabulary Mapping

        DOCUMENT_ID: doc_n1000_0302
        CATEGORY: Retrieval
        TAGS: vocabulary mapping, indexing pipelines

        ## Overview

This document covers vocabulary mapping in the context of local RAG systems. The approach loads indexing pipelines using typed techniques suitable for CPU-only environments.

ChunkStore reranks the core logic, ensuring defensive behavior across pipeline runs.

## Retrieval Pipeline

The vocabulary mapping retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-149 from each leg
3. **Score fusion**: weighted sum with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

indexing pipelines ensures no text or metadata enters the index backends directly.

## Validation Strategy

vocabulary mapping enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, lazy failure before expensive computation begins. The indexing pipelines validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.
