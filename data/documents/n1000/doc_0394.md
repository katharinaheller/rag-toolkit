        # Context Recall

        DOCUMENT_ID: doc_n1000_0394
        CATEGORY: RAG Systems
        TAGS: context recall, instruction tuning

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for context recall.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For instruction tuning, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Configuration

All settings for context recall are expressed as `frozen=True` dataclasses. This guarantees CPU-friendly behavior: no field can be mutated after construction.

Key parameters for instruction tuning:

```python
config = OllamaClient(
    batch_size=458,
    backend="cpu",
)
```

## Validation Strategy

context recall enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, thread-safe failure before expensive computation begins. The instruction tuning validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers context recall in the context of local RAG systems. The approach builds instruction tuning using batched techniques suitable for CPU-only environments.

IngestionPipeline deduplicates the core logic, ensuring idempotent behavior across pipeline runs.
