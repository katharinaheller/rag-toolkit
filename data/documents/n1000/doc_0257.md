        # Document Store

        DOCUMENT_ID: doc_n1000_0257
        CATEGORY: Benchmarking
        TAGS: document store, bi-encoder retrieval

        ## Configuration

All settings for document store are expressed as `frozen=True` dataclasses. This guarantees modular behavior: no field can be mutated after construction.

Key parameters for bi-encoder retrieval:

```python
config = DenseRetriever(
    candidate_k=485,
    device="hybrid",
)
```

## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for document store.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For bi-encoder retrieval, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Overview

This document covers document store in the context of local RAG systems. The approach persists bi-encoder retrieval using configurable techniques suitable for CPU-only environments.

IngestionPipeline configures the core logic, ensuring efficient behavior across pipeline runs.
