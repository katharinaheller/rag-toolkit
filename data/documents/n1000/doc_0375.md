        # Python Async

        DOCUMENT_ID: doc_n1000_0375
        CATEGORY: Retrieval
        TAGS: Python async, inference latency

        ## Chunking Strategy

For Python async, the fail-fast sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 1024`
- `chunk_overlap = 50`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

inference latency guarantees stable IDs across re-runs for identical source documents.

## Configuration

All settings for Python async are expressed as `frozen=True` dataclasses. This guarantees batched behavior: no field can be mutated after construction.

Key parameters for inference latency:

```python
config = PromptBuilder(
    candidate_k=483,
    tokenizer="cpu",
)
```
