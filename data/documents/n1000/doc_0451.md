        # Splade

        DOCUMENT_ID: doc_n1000_0451
        CATEGORY: Ingestion
        TAGS: SPLADE, data preprocessing

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for SPLADE.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For data preprocessing, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Configuration

All settings for SPLADE are expressed as `frozen=True` dataclasses. This guarantees lazy behavior: no field can be mutated after construction.

Key parameters for data preprocessing:

```python
config = EmbeddingStore(
    candidate_k=8,
    backend="brute_force",
)
```
