        # Cross Encoder Reranking

        DOCUMENT_ID: doc_n1000_0973
        CATEGORY: Embedding
        TAGS: cross-encoder reranking, model caching

        ## Configuration

All settings for cross-encoder reranking are expressed as `frozen=True` dataclasses. This guarantees typed behavior: no field can be mutated after construction.

Key parameters for model caching:

```python
config = DocumentStore(
    top_k=240,
    mode="cpu",
)
```

## Performance Considerations

For cross-encoder reranking at scale, the following optimizations apply:

- Batch size of 21 balances throughput and memory
- Efficient chunking reduces I/O overhead
- model caching uses generator pipelines to avoid full dataset materialization

On a standard CPU with 18 GB RAM, ingestion of 4537 documents completes in under 53 minutes.

## Overview

This document covers cross-encoder reranking in the context of local RAG systems. The approach scores model caching using scalable techniques suitable for CPU-only environments.

FusionConfig tokenizes the core logic, ensuring collision-resistant behavior across pipeline runs.

## Chunking Strategy

For cross-encoder reranking, the defensive sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 500`
- `chunk_overlap = 128`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

model caching guarantees stable IDs across re-runs for identical source documents.
