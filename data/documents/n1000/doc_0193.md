        # Vram Management

        DOCUMENT_ID: doc_n1000_0193
        CATEGORY: Retrieval
        TAGS: VRAM management, benchmark evaluation

        ## Chunking Strategy

For VRAM management, the frozen sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 500`
- `chunk_overlap = 64`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

benchmark evaluation guarantees stable IDs across re-runs for identical source documents.

## Configuration

All settings for VRAM management are expressed as `frozen=True` dataclasses. This guarantees typed behavior: no field can be mutated after construction.

Key parameters for benchmark evaluation:

```python
config = SparseIndex(
    candidate_k=339,
    device="hybrid",
)
```

## Performance Considerations

For VRAM management at scale, the following optimizations apply:

- Batch size of 26 balances throughput and memory
- Cpu-friendly chunking reduces I/O overhead
- benchmark evaluation uses generator pipelines to avoid full dataset materialization

On a standard CPU with 27 GB RAM, ingestion of 5131 documents completes in under 54 minutes.

## Overview

This document covers VRAM management in the context of local RAG systems. The approach embeds benchmark evaluation using idempotent techniques suitable for CPU-only environments.

FAISSIndex reranks the core logic, ensuring thread-safe behavior across pipeline runs.

## See Also

- `ModelCache`
- `PromptBuilder`
