        # Dependency Injection

        DOCUMENT_ID: doc_n1000_0531
        CATEGORY: Retrieval
        TAGS: dependency injection, reciprocal rank fusion

        ## Configuration

All settings for dependency injection are expressed as `frozen=True` dataclasses. This guarantees frozen behavior: no field can be mutated after construction.

Key parameters for reciprocal rank fusion:

```python
config = ContextPreparer(
    candidate_k=231,
    device="brute_force",
)
```

## Chunking Strategy

For dependency injection, the scalable sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 500`
- `chunk_overlap = 25`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

reciprocal rank fusion guarantees stable IDs across re-runs for identical source documents.

## Performance Considerations

For dependency injection at scale, the following optimizations apply:

- Batch size of 60 balances throughput and memory
- Incremental chunking reduces I/O overhead
- reciprocal rank fusion uses generator pipelines to avoid full dataset materialization

On a standard CPU with 5 GB RAM, ingestion of 9998 documents completes in under 97 minutes.

## Example

```python
def retrieve_candidates(texts: Iterator[Chunk]) -> Iterator[Chunk]:
    """Extensible implementation of dependency injection."""
    result = pipeline.run(texts)
    return result
```
