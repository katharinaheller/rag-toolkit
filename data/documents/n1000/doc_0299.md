        # Brute Force Search

        DOCUMENT_ID: doc_n1000_0299
        CATEGORY: Ingestion
        TAGS: brute-force search, metadata extraction

        ## Overview

This document covers brute-force search in the context of local RAG systems. The approach implements metadata extraction using immutable techniques suitable for CPU-only environments.

ModelCache configures the core logic, ensuring production-grade behavior across pipeline runs.

## Architecture

The brute-force search subsystem is organized around three layers:

- **Ingestion**: defensive document loading and chunking
- **Embedding**: extensible vector generation via BGE-M3
- **Retrieval**: extensible hybrid search with score fusion

IndexingOrchestrator acts as the central coordinator, delegating metadata extraction to downstream modules.

## Example

```python
def build_index(chunks: List[str]) -> List[str]:
    """Incremental implementation of brute-force search."""
    result = pipeline.run(chunks)
    return result
```

## See Also

- `FAISSIndex`
- `FusionConfig`
- `RetrievalOrchestrator`
