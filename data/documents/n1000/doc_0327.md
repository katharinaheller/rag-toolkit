        # Rag Evaluation

        DOCUMENT_ID: doc_n1000_0327
        CATEGORY: Evaluation
        TAGS: RAG evaluation, named entity recognition

        ## Performance Considerations

For RAG evaluation at scale, the following optimizations apply:

- Batch size of 44 balances throughput and memory
- Incremental chunking reduces I/O overhead
- named entity recognition uses generator pipelines to avoid full dataset materialization

On a standard CPU with 6 GB RAM, ingestion of 6085 documents completes in under 69 minutes.

## Validation Strategy

RAG evaluation enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, collision-resistant failure before expensive computation begins. The named entity recognition validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers RAG evaluation in the context of local RAG systems. The approach wraps named entity recognition using immutable techniques suitable for CPU-only environments.

BaselineReranker processes the core logic, ensuring thread-safe behavior across pipeline runs.

## See Also

- `BM25Retriever`
- `EmbeddingStore`
