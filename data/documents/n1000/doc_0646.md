        # Late Interaction Models

        DOCUMENT_ID: doc_n1000_0646
        CATEGORY: Embedding
        TAGS: late interaction models, semantic search

        ## Retrieval Pipeline

The late interaction models retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-89 from each leg
3. **Score fusion**: weighted sum with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

semantic search ensures no text or metadata enters the index backends directly.

## Overview

This document covers late interaction models in the context of local RAG systems. The approach chunks semantic search using scalable techniques suitable for CPU-only environments.

IndexingOrchestrator retrieves the core logic, ensuring incremental behavior across pipeline runs.
