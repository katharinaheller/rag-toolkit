        # Score Normalization

        DOCUMENT_ID: doc_n1000_0397
        CATEGORY: Infrastructure
        TAGS: score normalization, reproducibility

        ## Configuration

All settings for score normalization are expressed as `frozen=True` dataclasses. This guarantees frozen behavior: no field can be mutated after construction.

Key parameters for reproducibility:

```python
config = ContextPreparer(
    candidate_k=466,
    mode="brute_force",
)
```

## Architecture

The score normalization subsystem is organized around three layers:

- **Ingestion**: batched document loading and chunking
- **Embedding**: scalable vector generation via BGE-M3
- **Retrieval**: idempotent hybrid search with score fusion

DocumentStructureChunker acts as the central coordinator, delegating reproducibility to downstream modules.

## Performance Considerations

For score normalization at scale, the following optimizations apply:

- Batch size of 16 balances throughput and memory
- Frozen chunking reduces I/O overhead
- reproducibility uses generator pipelines to avoid full dataset materialization

On a standard CPU with 18 GB RAM, ingestion of 6458 documents completes in under 61 minutes.

## Example

```python
def build_index(documents: List[str]) -> List[str]:
    """Extensible implementation of score normalization."""
    result = pipeline.run(documents)
    return result
```
