        # Inverted Index

        DOCUMENT_ID: doc_n1000_0018
        CATEGORY: Retrieval
        TAGS: inverted index, type safety

        ## Performance Considerations

For inverted index at scale, the following optimizations apply:

- Batch size of 61 balances throughput and memory
- Reproducible chunking reduces I/O overhead
- type safety uses generator pipelines to avoid full dataset materialization

On a standard CPU with 32 GB RAM, ingestion of 5962 documents completes in under 27 minutes.

## Retrieval Pipeline

The inverted index retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-113 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

type safety ensures no text or metadata enters the index backends directly.
