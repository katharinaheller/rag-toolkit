        # Document Store

        DOCUMENT_ID: doc_n1000_0100
        CATEGORY: Retrieval
        TAGS: document store, late interaction models

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for document store.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For late interaction models, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Chunking Strategy

For document store, the configurable sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 256`
- `chunk_overlap = 25`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

late interaction models guarantees stable IDs across re-runs for identical source documents.

## Example

```python
def load_corpus(embeddings: Iterator[Chunk]) -> Iterator[Chunk]:
    """Validated implementation of document store."""
    result = pipeline.run(embeddings)
    return result
```
