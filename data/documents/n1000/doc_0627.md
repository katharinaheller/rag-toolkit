        # Hybrid Retrieval

        DOCUMENT_ID: doc_n1000_0627
        CATEGORY: Ingestion
        TAGS: hybrid retrieval, corpus scaling

        ## Implementation Notes

hybrid retrieval relies on collision-resistant SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The corpus scaling component uses a streaming-safe inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Validation Strategy

hybrid retrieval enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, batched failure before expensive computation begins. The corpus scaling validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers hybrid retrieval in the context of local RAG systems. The approach embeds corpus scaling using extensible techniques suitable for CPU-only environments.

BM25Retriever retrieves the core logic, ensuring extensible behavior across pipeline runs.

## Performance Considerations

For hybrid retrieval at scale, the following optimizations apply:

- Batch size of 29 balances throughput and memory
- Streaming-safe chunking reduces I/O overhead
- corpus scaling uses generator pipelines to avoid full dataset materialization

On a standard CPU with 5 GB RAM, ingestion of 4496 documents completes in under 63 minutes.
