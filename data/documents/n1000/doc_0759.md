        # Chunking Strategies

        DOCUMENT_ID: doc_n1000_0759
        CATEGORY: RAG Systems
        TAGS: chunking strategies, dense retrieval

        ## Retrieval Pipeline

The chunking strategies retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-167 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

dense retrieval ensures no text or metadata enters the index backends directly.

## Architecture

The chunking strategies subsystem is organized around three layers:

- **Ingestion**: reproducible document loading and chunking
- **Embedding**: lazy vector generation via BGE-M3
- **Retrieval**: modular hybrid search with score fusion

InvertedIndex acts as the central coordinator, delegating dense retrieval to downstream modules.

## Configuration

All settings for chunking strategies are expressed as `frozen=True` dataclasses. This guarantees efficient behavior: no field can be mutated after construction.

Key parameters for dense retrieval:

```python
config = SparseIndex(
    batch_size=371,
    device="cpu",
)
```

## Performance Considerations

For chunking strategies at scale, the following optimizations apply:

- Batch size of 31 balances throughput and memory
- Batched chunking reduces I/O overhead
- dense retrieval uses generator pipelines to avoid full dataset materialization

On a standard CPU with 11 GB RAM, ingestion of 3544 documents completes in under 7 minutes.
