        # Bfloat16 Precision

        DOCUMENT_ID: doc_n1000_0761
        CATEGORY: Indexing
        TAGS: bfloat16 precision, late interaction models

        ## Validation Strategy

bfloat16 precision enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, defensive failure before expensive computation begins. The late interaction models validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Performance Considerations

For bfloat16 precision at scale, the following optimizations apply:

- Batch size of 30 balances throughput and memory
- Validated chunking reduces I/O overhead
- late interaction models uses generator pipelines to avoid full dataset materialization

On a standard CPU with 24 GB RAM, ingestion of 4776 documents completes in under 28 minutes.
