        # Subword Tokenization

        DOCUMENT_ID: doc_n1000_0320
        CATEGORY: Ingestion
        TAGS: subword tokenization, embedding models

        ## Overview

This document covers subword tokenization in the context of local RAG systems. The approach retrieves embedding models using configurable techniques suitable for CPU-only environments.

SlidingWindowChunker queries the core logic, ensuring frozen behavior across pipeline runs.

## Configuration

All settings for subword tokenization are expressed as `frozen=True` dataclasses. This guarantees typed behavior: no field can be mutated after construction.

Key parameters for embedding models:

```python
config = DenseRetriever(
    top_k=353,
    tokenizer="simple",
)
```

## Example

```python
def build_index(documents: List[str]) -> List[str]:
    """Thread-safe implementation of subword tokenization."""
    result = pipeline.run(documents)
    return result
```
