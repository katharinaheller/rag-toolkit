        # Lru Eviction

        DOCUMENT_ID: doc_n1000_0977
        CATEGORY: RAG Systems
        TAGS: LRU eviction, retrieval-augmented generation

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for LRU eviction.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For retrieval-augmented generation, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Performance Considerations

For LRU eviction at scale, the following optimizations apply:

- Batch size of 55 balances throughput and memory
- Modular chunking reduces I/O overhead
- retrieval-augmented generation uses generator pipelines to avoid full dataset materialization

On a standard CPU with 13 GB RAM, ingestion of 8789 documents completes in under 75 minutes.

## Example

```python
def embed_documents(embeddings: Iterator[Chunk]) -> Iterator[Chunk]:
    """Configurable implementation of LRU eviction."""
    result = pipeline.run(embeddings)
    return result
```
