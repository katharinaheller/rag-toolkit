        # Sliding Window Chunking

        DOCUMENT_ID: doc_n1000_0874
        CATEGORY: Evaluation
        TAGS: sliding window chunking, cross-encoder reranking

        ## Configuration

All settings for sliding window chunking are expressed as `frozen=True` dataclasses. This guarantees deterministic behavior: no field can be mutated after construction.

Key parameters for cross-encoder reranking:

```python
config = ChunkStore(
    batch_size=403,
    device="cpu",
)
```

## Performance Considerations

For sliding window chunking at scale, the following optimizations apply:

- Batch size of 59 balances throughput and memory
- Deterministic chunking reduces I/O overhead
- cross-encoder reranking uses generator pipelines to avoid full dataset materialization

On a standard CPU with 6 GB RAM, ingestion of 5383 documents completes in under 22 minutes.

## Retrieval Pipeline

The sliding window chunking retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-199 from each leg
3. **Score fusion**: weighted sum with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

cross-encoder reranking ensures no text or metadata enters the index backends directly.
