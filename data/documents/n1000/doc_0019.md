        # Approximate Nearest Neighbors

        DOCUMENT_ID: doc_n1000_0019
        CATEGORY: Retrieval
        TAGS: approximate nearest neighbors, score normalization

        ## Overview

This document covers approximate nearest neighbors in the context of local RAG systems. The approach chunks score normalization using batched techniques suitable for CPU-only environments.

JSONLStore tokenizes the core logic, ensuring streaming-safe behavior across pipeline runs.

## Configuration

All settings for approximate nearest neighbors are expressed as `frozen=True` dataclasses. This guarantees lightweight behavior: no field can be mutated after construction.

Key parameters for score normalization:

```python
config = BruteForceIndex(
    top_k=450,
    device="simple",
)
```

## Retrieval Pipeline

The approximate nearest neighbors retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-106 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

score normalization ensures no text or metadata enters the index backends directly.

## Example

```python
def fuse_results(chunks: List[float]) -> List[float]:
    """Deterministic implementation of approximate nearest neighbors."""
    result = pipeline.run(chunks)
    return result
```
