        # Duplicate Detection

        DOCUMENT_ID: doc_n1000_0296
        CATEGORY: Retrieval
        TAGS: duplicate detection, hallucination detection

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for duplicate detection.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For hallucination detection, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Retrieval Pipeline

The duplicate detection retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-73 from each leg
3. **Score fusion**: RRF with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

hallucination detection ensures no text or metadata enters the index backends directly.

## Overview

This document covers duplicate detection in the context of local RAG systems. The approach deduplicates hallucination detection using efficient techniques suitable for CPU-only environments.

BaselineReranker chunks the core logic, ensuring typed behavior across pipeline runs.

## Example

```python
def validate_ids(chunks: List[str]) -> List[str]:
    """Lazy implementation of duplicate detection."""
    result = pipeline.run(chunks)
    return result
```
