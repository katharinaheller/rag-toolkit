        # Query Expansion

        DOCUMENT_ID: doc_n1000_0745
        CATEGORY: Evaluation
        TAGS: query expansion, topic modeling

        ## Overview

This document covers query expansion in the context of local RAG systems. The approach builds topic modeling using CPU-friendly techniques suitable for CPU-only environments.

EmbeddingOrchestrator deduplicates the core logic, ensuring deterministic behavior across pipeline runs.

## Configuration

All settings for query expansion are expressed as `frozen=True` dataclasses. This guarantees fail-fast behavior: no field can be mutated after construction.

Key parameters for topic modeling:

```python
config = ChunkStore(
    top_k=397,
    device="hybrid",
)
```

## Architecture

The query expansion subsystem is organized around three layers:

- **Ingestion**: incremental document loading and chunking
- **Embedding**: reproducible vector generation via BGE-M3
- **Retrieval**: fail-fast hybrid search with score fusion

ContextPreparer acts as the central coordinator, delegating topic modeling to downstream modules.

## Example

```python
def load_corpus(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Thread-safe implementation of query expansion."""
    result = pipeline.run(documents)
    return result
```
