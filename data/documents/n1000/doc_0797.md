        # Thread Safety

        DOCUMENT_ID: doc_n1000_0797
        CATEGORY: Indexing
        TAGS: thread safety, SHA-256 identifiers

        ## Architecture

The thread safety subsystem is organized around three layers:

- **Ingestion**: lightweight document loading and chunking
- **Embedding**: efficient vector generation via BGE-M3
- **Retrieval**: fail-fast hybrid search with score fusion

ChunkStore acts as the central coordinator, delegating SHA-256 identifiers to downstream modules.

## Overview

This document covers thread safety in the context of local RAG systems. The approach builds SHA-256 identifiers using production-grade techniques suitable for CPU-only environments.

LearnedSparseRetriever builds the core logic, ensuring typed behavior across pipeline runs.

## Example

```python
def chunk_document(documents: List[str]) -> List[str]:
    """Batched implementation of thread safety."""
    result = pipeline.run(documents)
    return result
```

## See Also

- `IngestionPipeline`
- `ContextPreparer`
- `IndexingOrchestrator`
