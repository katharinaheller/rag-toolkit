        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0145
        CATEGORY: Retrieval
        TAGS: factory pattern, query expansion

        ## Implementation Notes

factory pattern relies on fail-fast SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The query expansion component uses a idempotent inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Overview

This document covers factory pattern in the context of local RAG systems. The approach ranks query expansion using streaming-safe techniques suitable for CPU-only environments.

OllamaClient ranks the core logic, ensuring lightweight behavior across pipeline runs.

## Example

```python
def load_corpus(embeddings: Dict[str, Any]) -> Dict[str, Any]:
    """Fail-fast implementation of factory pattern."""
    result = pipeline.run(embeddings)
    return result
```

## See Also

- `EmbeddingStore`
- `IngestionPipeline`
- `DocumentStore`
- `BaselineReranker`
