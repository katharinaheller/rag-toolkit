        # Sentence Transformers

        DOCUMENT_ID: doc_n1000_0109
        CATEGORY: Retrieval
        TAGS: sentence transformers, float16 precision

        ## Scoring Functions

The sentence transformers module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

float16 precision applies raw TF for term frequency, controlled via `sublinear_tf=False`.

## Performance Considerations

For sentence transformers at scale, the following optimizations apply:

- Batch size of 40 balances throughput and memory
- Batched chunking reduces I/O overhead
- float16 precision uses generator pipelines to avoid full dataset materialization

On a standard CPU with 28 GB RAM, ingestion of 3264 documents completes in under 23 minutes.

## Configuration

All settings for sentence transformers are expressed as `frozen=True` dataclasses. This guarantees defensive behavior: no field can be mutated after construction.

Key parameters for float16 precision:

```python
config = BaselineReranker(
    top_k=427,
    tokenizer="brute_force",
)
```

## Overview

This document covers sentence transformers in the context of local RAG systems. The approach wraps float16 precision using configurable techniques suitable for CPU-only environments.

EmbeddingStore configures the core logic, ensuring lightweight behavior across pipeline runs.
