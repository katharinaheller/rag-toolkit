        # Bi Encoder Retrieval

        DOCUMENT_ID: doc_n1000_0907
        CATEGORY: Benchmarking
        TAGS: bi-encoder retrieval, LRU eviction

        ## Overview

This document covers bi-encoder retrieval in the context of local RAG systems. The approach processes LRU eviction using streaming-safe techniques suitable for CPU-only environments.

EmbeddingOrchestrator deduplicates the core logic, ensuring validated behavior across pipeline runs.

## Configuration

All settings for bi-encoder retrieval are expressed as `frozen=True` dataclasses. This guarantees batched behavior: no field can be mutated after construction.

Key parameters for LRU eviction:

```python
config = EmbeddingStore(
    candidate_k=38,
    mode="hybrid",
)
```

## Architecture

The bi-encoder retrieval subsystem is organized around three layers:

- **Ingestion**: production-grade document loading and chunking
- **Embedding**: immutable vector generation via BGE-M3
- **Retrieval**: CPU-friendly hybrid search with score fusion

RetrievalOrchestrator acts as the central coordinator, delegating LRU eviction to downstream modules.
