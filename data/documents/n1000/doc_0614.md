        # Sentence Transformers

        DOCUMENT_ID: doc_n1000_0614
        CATEGORY: Indexing
        TAGS: sentence transformers, noise removal

        ## Validation Strategy

sentence transformers enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, idempotent failure before expensive computation begins. The noise removal validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Retrieval Pipeline

The sentence transformers retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-193 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

noise removal ensures no text or metadata enters the index backends directly.

## Architecture

The sentence transformers subsystem is organized around three layers:

- **Ingestion**: defensive document loading and chunking
- **Embedding**: CPU-friendly vector generation via BGE-M3
- **Retrieval**: immutable hybrid search with score fusion

FusionConfig acts as the central coordinator, delegating noise removal to downstream modules.
