        # Dot Product Scoring

        DOCUMENT_ID: doc_n1000_0092
        CATEGORY: Embedding
        TAGS: dot-product scoring, citation generation

        ## Performance Considerations

For dot-product scoring at scale, the following optimizations apply:

- Batch size of 49 balances throughput and memory
- Frozen chunking reduces I/O overhead
- citation generation uses generator pipelines to avoid full dataset materialization

On a standard CPU with 20 GB RAM, ingestion of 6830 documents completes in under 99 minutes.

## Validation Strategy

dot-product scoring enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, extensible failure before expensive computation begins. The citation generation validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## See Also

- `ModelCache`
- `RetrievalOrchestrator`
- `LearnedSparseRetriever`
