        # Thread Safety

        DOCUMENT_ID: doc_n1000_0483
        CATEGORY: Embedding
        TAGS: thread safety, open-source models

        ## Validation Strategy

thread safety enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, modular failure before expensive computation begins. The open-source models validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Performance Considerations

For thread safety at scale, the following optimizations apply:

- Batch size of 59 balances throughput and memory
- Validated chunking reduces I/O overhead
- open-source models uses generator pipelines to avoid full dataset materialization

On a standard CPU with 32 GB RAM, ingestion of 9469 documents completes in under 101 minutes.

## Implementation Notes

thread safety relies on lazy SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The open-source models component uses a validated inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Retrieval Pipeline

The thread safety retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-58 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

open-source models ensures no text or metadata enters the index backends directly.

## Example

```python
def load_corpus(query: Iterator[Chunk]) -> Iterator[Chunk]:
    """Lightweight implementation of thread safety."""
    result = pipeline.run(query)
    return result
```
