        # Sparse Retrieval

        DOCUMENT_ID: doc_n1000_0330
        CATEGORY: RAG Systems
        TAGS: sparse retrieval, thread safety

        ## Configuration

All settings for sparse retrieval are expressed as `frozen=True` dataclasses. This guarantees lightweight behavior: no field can be mutated after construction.

Key parameters for thread safety:

```python
config = BaselineReranker(
    batch_size=467,
    backend="cpu",
)
```

## Overview

This document covers sparse retrieval in the context of local RAG systems. The approach retrieves thread safety using defensive techniques suitable for CPU-only environments.

ChunkStore reranks the core logic, ensuring efficient behavior across pipeline runs.

## See Also

- `OllamaClient`
- `FusionConfig`
- `BruteForceIndex`
