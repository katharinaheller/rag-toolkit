        # Reciprocal Rank Fusion

        DOCUMENT_ID: doc_n1000_0812
        CATEGORY: Infrastructure
        TAGS: reciprocal rank fusion, cross-encoder reranking

        ## Validation Strategy

reciprocal rank fusion enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, batched failure before expensive computation begins. The cross-encoder reranking validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers reciprocal rank fusion in the context of local RAG systems. The approach fuses cross-encoder reranking using extensible techniques suitable for CPU-only environments.

FusionConfig scores the core logic, ensuring collision-resistant behavior across pipeline runs.

## Performance Considerations

For reciprocal rank fusion at scale, the following optimizations apply:

- Batch size of 24 balances throughput and memory
- Deterministic chunking reduces I/O overhead
- cross-encoder reranking uses generator pipelines to avoid full dataset materialization

On a standard CPU with 19 GB RAM, ingestion of 8415 documents completes in under 79 minutes.

## Architecture

The reciprocal rank fusion subsystem is organized around three layers:

- **Ingestion**: configurable document loading and chunking
- **Embedding**: immutable vector generation via BGE-M3
- **Retrieval**: defensive hybrid search with score fusion

InvertedIndex acts as the central coordinator, delegating cross-encoder reranking to downstream modules.

## Example

```python
def load_corpus(texts: Iterator[Chunk]) -> Iterator[Chunk]:
    """Lightweight implementation of reciprocal rank fusion."""
    result = pipeline.run(texts)
    return result
```
