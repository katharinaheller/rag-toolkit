        # Answer Faithfulness

        DOCUMENT_ID: doc_n1000_0552
        CATEGORY: RAG Systems
        TAGS: answer faithfulness, reciprocal rank fusion

        ## Chunking Strategy

For answer faithfulness, the modular sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 1024`
- `chunk_overlap = 64`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

reciprocal rank fusion guarantees stable IDs across re-runs for identical source documents.

## Architecture

The answer faithfulness subsystem is organized around three layers:

- **Ingestion**: lightweight document loading and chunking
- **Embedding**: CPU-friendly vector generation via BGE-M3
- **Retrieval**: incremental hybrid search with score fusion

BruteForceIndex acts as the central coordinator, delegating reciprocal rank fusion to downstream modules.

## Validation Strategy

answer faithfulness enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, thread-safe failure before expensive computation begins. The reciprocal rank fusion validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers answer faithfulness in the context of local RAG systems. The approach streams reciprocal rank fusion using typed techniques suitable for CPU-only environments.

BruteForceIndex deserializes the core logic, ensuring configurable behavior across pipeline runs.
