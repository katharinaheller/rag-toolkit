        # Learned Sparse Representations

        DOCUMENT_ID: doc_n1000_0565
        CATEGORY: Benchmarking
        TAGS: learned sparse representations, context recall

        ## Configuration

All settings for learned sparse representations are expressed as `frozen=True` dataclasses. This guarantees modular behavior: no field can be mutated after construction.

Key parameters for context recall:

```python
config = LoaderRegistry(
    top_k=330,
    tokenizer="cpu",
)
```

## Retrieval Pipeline

The learned sparse representations retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-183 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

context recall ensures no text or metadata enters the index backends directly.
