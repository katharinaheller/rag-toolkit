        # Open Source Models

        DOCUMENT_ID: doc_n1000_0700
        CATEGORY: Indexing
        TAGS: open-source models, zero-shot inference

        ## Configuration

All settings for open-source models are expressed as `frozen=True` dataclasses. This guarantees CPU-friendly behavior: no field can be mutated after construction.

Key parameters for zero-shot inference:

```python
config = HybridRetriever(
    candidate_k=17,
    mode="simple",
)
```

## Validation Strategy

open-source models enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, scalable failure before expensive computation begins. The zero-shot inference validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Performance Considerations

For open-source models at scale, the following optimizations apply:

- Batch size of 57 balances throughput and memory
- Configurable chunking reduces I/O overhead
- zero-shot inference uses generator pipelines to avoid full dataset materialization

On a standard CPU with 4 GB RAM, ingestion of 7373 documents completes in under 112 minutes.
