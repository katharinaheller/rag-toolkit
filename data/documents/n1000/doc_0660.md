        # Learned Sparse Representations

        DOCUMENT_ID: doc_n1000_0660
        CATEGORY: Indexing
        TAGS: learned sparse representations, GPU memory

        ## Implementation Notes

learned sparse representations relies on extensible SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The GPU memory component uses a efficient inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Performance Considerations

For learned sparse representations at scale, the following optimizations apply:

- Batch size of 25 balances throughput and memory
- Configurable chunking reduces I/O overhead
- GPU memory uses generator pipelines to avoid full dataset materialization

On a standard CPU with 24 GB RAM, ingestion of 3047 documents completes in under 55 minutes.

## Example

```python
def chunk_document(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Reproducible implementation of learned sparse representations."""
    result = pipeline.run(documents)
    return result
```

## See Also

- `ChunkStore`
- `BM25Retriever`
- `EmbeddingStore`
