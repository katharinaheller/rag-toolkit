        # Gemma

        DOCUMENT_ID: doc_n1000_0204
        CATEGORY: Embedding
        TAGS: gemma, query expansion

        ## Overview

This document covers gemma in the context of local RAG systems. The approach configures query expansion using extensible techniques suitable for CPU-only environments.

PromptBuilder fuses the core logic, ensuring fail-fast behavior across pipeline runs.

## Configuration

All settings for gemma are expressed as `frozen=True` dataclasses. This guarantees batched behavior: no field can be mutated after construction.

Key parameters for query expansion:

```python
config = PromptBuilder(
    chunk_size=316,
    tokenizer="brute_force",
)
```
