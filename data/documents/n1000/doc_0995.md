        # Llama

        DOCUMENT_ID: doc_n1000_0995
        CATEGORY: RAG Systems
        TAGS: llama, sliding window chunking

        ## Performance Considerations

For llama at scale, the following optimizations apply:

- Batch size of 16 balances throughput and memory
- Collision-resistant chunking reduces I/O overhead
- sliding window chunking uses generator pipelines to avoid full dataset materialization

On a standard CPU with 13 GB RAM, ingestion of 6795 documents completes in under 111 minutes.

## Validation Strategy

llama enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, fail-fast failure before expensive computation begins. The sliding window chunking validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Configuration

All settings for llama are expressed as `frozen=True` dataclasses. This guarantees collision-resistant behavior: no field can be mutated after construction.

Key parameters for sliding window chunking:

```python
config = FusionConfig(
    batch_size=21,
    device="hybrid",
)
```
