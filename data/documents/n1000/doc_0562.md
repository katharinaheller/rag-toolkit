        # Answer Faithfulness

        DOCUMENT_ID: doc_n1000_0562
        CATEGORY: Indexing
        TAGS: answer faithfulness, reproducibility

        ## Overview

This document covers answer faithfulness in the context of local RAG systems. The approach reranks reproducibility using deterministic techniques suitable for CPU-only environments.

LearnedSparseRetriever queries the core logic, ensuring modular behavior across pipeline runs.

## Performance Considerations

For answer faithfulness at scale, the following optimizations apply:

- Batch size of 57 balances throughput and memory
- Configurable chunking reduces I/O overhead
- reproducibility uses generator pipelines to avoid full dataset materialization

On a standard CPU with 5 GB RAM, ingestion of 8840 documents completes in under 118 minutes.

## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for answer faithfulness.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For reproducibility, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Retrieval Pipeline

The answer faithfulness retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-185 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

reproducibility ensures no text or metadata enters the index backends directly.

## Example

```python
def embed_documents(documents: Dict[str, Any]) -> Dict[str, Any]:
    """Streaming-safe implementation of answer faithfulness."""
    result = pipeline.run(documents)
    return result
```
