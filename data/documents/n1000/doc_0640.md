        # Chain Of Thought Prompting

        DOCUMENT_ID: doc_n1000_0640
        CATEGORY: Ingestion
        TAGS: chain-of-thought prompting, streaming pipelines

        ## Embedding Model Details

BGE-M3 (BAAI/bge-m3) is the primary embedding model for chain-of-thought prompting.

| Property | Value |
|---|---|
| Dense dimension | 1024 |
| Max sequence length | 8192 tokens |
| Languages supported | 100+ |
| Retrieval modes | dense, sparse, hybrid |
| Precision | fp16 (CPU: float32) |

For streaming pipelines, `use_fp16=False` and `use_bfloat16=False` must be set on CPU. FlagEmbedding's fp16 path is invalid on most CPU backends.

## Performance Considerations

For chain-of-thought prompting at scale, the following optimizations apply:

- Batch size of 43 balances throughput and memory
- Configurable chunking reduces I/O overhead
- streaming pipelines uses generator pipelines to avoid full dataset materialization

On a standard CPU with 12 GB RAM, ingestion of 9293 documents completes in under 70 minutes.

## Retrieval Pipeline

The chain-of-thought prompting retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-101 from each leg
3. **Score fusion**: weighted sum with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

streaming pipelines ensures no text or metadata enters the index backends directly.

## See Also

- `PromptBuilder`
- `IngestionPipeline`
- `DocumentStore`
