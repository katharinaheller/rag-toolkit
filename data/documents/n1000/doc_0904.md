        # Hybrid Retrieval

        DOCUMENT_ID: doc_n1000_0904
        CATEGORY: Embedding
        TAGS: hybrid retrieval, embedding models

        ## Chunking Strategy

For hybrid retrieval, the efficient sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 512`
- `chunk_overlap = 25`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

embedding models guarantees stable IDs across re-runs for identical source documents.

## Overview

This document covers hybrid retrieval in the context of local RAG systems. The approach deserializes embedding models using collision-resistant techniques suitable for CPU-only environments.

IndexingOrchestrator retrieves the core logic, ensuring extensible behavior across pipeline runs.

## Performance Considerations

For hybrid retrieval at scale, the following optimizations apply:

- Batch size of 53 balances throughput and memory
- Lightweight chunking reduces I/O overhead
- embedding models uses generator pipelines to avoid full dataset materialization

On a standard CPU with 31 GB RAM, ingestion of 8649 documents completes in under 26 minutes.
