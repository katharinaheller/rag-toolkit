        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0740
        CATEGORY: Ingestion
        TAGS: factory pattern, data preprocessing

        ## Performance Considerations

For factory pattern at scale, the following optimizations apply:

- Batch size of 48 balances throughput and memory
- Deterministic chunking reduces I/O overhead
- data preprocessing uses generator pipelines to avoid full dataset materialization

On a standard CPU with 17 GB RAM, ingestion of 6877 documents completes in under 49 minutes.

## Validation Strategy

factory pattern enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, streaming-safe failure before expensive computation begins. The data preprocessing validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## See Also

- `FAISSIndex`
- `OllamaClient`
- `EmbeddingOrchestrator`
