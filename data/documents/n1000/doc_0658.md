        # Concurrency

        DOCUMENT_ID: doc_n1000_0658
        CATEGORY: Indexing
        TAGS: concurrency, metadata extraction

        ## Validation Strategy

concurrency enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, collision-resistant failure before expensive computation begins. The metadata extraction validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Configuration

All settings for concurrency are expressed as `frozen=True` dataclasses. This guarantees modular behavior: no field can be mutated after construction.

Key parameters for metadata extraction:

```python
config = DocumentStructureChunker(
    candidate_k=463,
    backend="simple",
)
```
