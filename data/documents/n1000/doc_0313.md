        # Throughput Optimization

        DOCUMENT_ID: doc_n1000_0313
        CATEGORY: Infrastructure
        TAGS: throughput optimization, benchmark evaluation

        ## Retrieval Pipeline

The throughput optimization retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-180 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

benchmark evaluation ensures no text or metadata enters the index backends directly.

## Architecture

The throughput optimization subsystem is organized around three layers:

- **Ingestion**: reproducible document loading and chunking
- **Embedding**: idempotent vector generation via BGE-M3
- **Retrieval**: typed hybrid search with score fusion

IndexingOrchestrator acts as the central coordinator, delegating benchmark evaluation to downstream modules.

## Overview

This document covers throughput optimization in the context of local RAG systems. The approach implements benchmark evaluation using lazy techniques suitable for CPU-only environments.

ContextPreparer normalizes the core logic, ensuring scalable behavior across pipeline runs.

## Performance Considerations

For throughput optimization at scale, the following optimizations apply:

- Batch size of 59 balances throughput and memory
- Thread-safe chunking reduces I/O overhead
- benchmark evaluation uses generator pipelines to avoid full dataset materialization

On a standard CPU with 31 GB RAM, ingestion of 610 documents completes in under 31 minutes.
