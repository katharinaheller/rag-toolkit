        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0666
        CATEGORY: RAG Systems
        TAGS: factory pattern, SHA-256 identifiers

        ## Performance Considerations

For factory pattern at scale, the following optimizations apply:

- Batch size of 57 balances throughput and memory
- Extensible chunking reduces I/O overhead
- SHA-256 identifiers uses generator pipelines to avoid full dataset materialization

On a standard CPU with 7 GB RAM, ingestion of 3239 documents completes in under 74 minutes.

## Architecture

The factory pattern subsystem is organized around three layers:

- **Ingestion**: streaming-safe document loading and chunking
- **Embedding**: thread-safe vector generation via BGE-M3
- **Retrieval**: defensive hybrid search with score fusion

DocumentStructureChunker acts as the central coordinator, delegating SHA-256 identifiers to downstream modules.
