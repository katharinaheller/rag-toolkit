        # Cross Encoder Reranking

        DOCUMENT_ID: doc_n1000_0319
        CATEGORY: Benchmarking
        TAGS: cross-encoder reranking, reciprocal rank fusion

        ## Configuration

All settings for cross-encoder reranking are expressed as `frozen=True` dataclasses. This guarantees frozen behavior: no field can be mutated after construction.

Key parameters for reciprocal rank fusion:

```python
config = SparseIndex(
    batch_size=179,
    device="hybrid",
)
```

## Performance Considerations

For cross-encoder reranking at scale, the following optimizations apply:

- Batch size of 42 balances throughput and memory
- Modular chunking reduces I/O overhead
- reciprocal rank fusion uses generator pipelines to avoid full dataset materialization

On a standard CPU with 30 GB RAM, ingestion of 8225 documents completes in under 97 minutes.

## Implementation Notes

cross-encoder reranking relies on extensible SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The reciprocal rank fusion component uses a configurable inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Example

```python
def validate_ids(chunks: List[float]) -> List[float]:
    """Fail-fast implementation of cross-encoder reranking."""
    result = pipeline.run(chunks)
    return result
```
