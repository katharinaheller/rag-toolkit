        # Bfloat16 Precision

        DOCUMENT_ID: doc_n1000_0324
        CATEGORY: Retrieval
        TAGS: bfloat16 precision, model quantization

        ## Performance Considerations

For bfloat16 precision at scale, the following optimizations apply:

- Batch size of 28 balances throughput and memory
- Configurable chunking reduces I/O overhead
- model quantization uses generator pipelines to avoid full dataset materialization

On a standard CPU with 14 GB RAM, ingestion of 1084 documents completes in under 34 minutes.

## Overview

This document covers bfloat16 precision in the context of local RAG systems. The approach indexes model quantization using lightweight techniques suitable for CPU-only environments.

LearnedSparseRetriever wraps the core logic, ensuring frozen behavior across pipeline runs.

## See Also

- `ChunkStore`
- `ModelCache`
- `SlidingWindowChunker`
