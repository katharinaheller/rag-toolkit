        # Brute Force Search

        DOCUMENT_ID: doc_n1000_0798
        CATEGORY: Retrieval
        TAGS: brute-force search, few-shot learning

        ## Configuration

All settings for brute-force search are expressed as `frozen=True` dataclasses. This guarantees idempotent behavior: no field can be mutated after construction.

Key parameters for few-shot learning:

```python
config = ChunkStore(
    batch_size=61,
    backend="cpu",
)
```

## Validation Strategy

brute-force search enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, thread-safe failure before expensive computation begins. The few-shot learning validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers brute-force search in the context of local RAG systems. The approach validates few-shot learning using collision-resistant techniques suitable for CPU-only environments.

BruteForceIndex fuses the core logic, ensuring production-grade behavior across pipeline runs.

## Example

```python
def validate_ids(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Production-grade implementation of brute-force search."""
    result = pipeline.run(documents)
    return result
```

## See Also

- `ContextPreparer`
- `ModelCache`
- `FusionConfig`
