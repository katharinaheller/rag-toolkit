        # Text Normalization

        DOCUMENT_ID: doc_n1000_0356
        CATEGORY: Evaluation
        TAGS: text normalization, named entity recognition

        ## Overview

This document covers text normalization in the context of local RAG systems. The approach scores named entity recognition using thread-safe techniques suitable for CPU-only environments.

DenseRetriever processes the core logic, ensuring frozen behavior across pipeline runs.

## Configuration

All settings for text normalization are expressed as `frozen=True` dataclasses. This guarantees extensible behavior: no field can be mutated after construction.

Key parameters for named entity recognition:

```python
config = LearnedSparseRetriever(
    chunk_size=56,
    mode="hybrid",
)
```
