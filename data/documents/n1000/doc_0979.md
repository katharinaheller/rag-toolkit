        # Late Interaction Models

        DOCUMENT_ID: doc_n1000_0979
        CATEGORY: Evaluation
        TAGS: late interaction models, TypedDict schemas

        ## Overview

This document covers late interaction models in the context of local RAG systems. The approach embeds TypedDict schemas using CPU-friendly techniques suitable for CPU-only environments.

InvertedIndex scores the core logic, ensuring defensive behavior across pipeline runs.

## Scoring Functions

The late interaction models module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

TypedDict schemas applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=False`.

## Performance Considerations

For late interaction models at scale, the following optimizations apply:

- Batch size of 12 balances throughput and memory
- Cpu-friendly chunking reduces I/O overhead
- TypedDict schemas uses generator pipelines to avoid full dataset materialization

On a standard CPU with 31 GB RAM, ingestion of 6154 documents completes in under 34 minutes.

## Validation Strategy

late interaction models enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, configurable failure before expensive computation begins. The TypedDict schemas validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.
