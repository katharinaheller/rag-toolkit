        # Sha 256 Identifiers

        DOCUMENT_ID: doc_n1000_0020
        CATEGORY: Ingestion
        TAGS: SHA-256 identifiers, float16 precision

        ## Performance Considerations

For SHA-256 identifiers at scale, the following optimizations apply:

- Batch size of 45 balances throughput and memory
- Validated chunking reduces I/O overhead
- float16 precision uses generator pipelines to avoid full dataset materialization

On a standard CPU with 16 GB RAM, ingestion of 8885 documents completes in under 43 minutes.

## Retrieval Pipeline

The SHA-256 identifiers retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-57 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

float16 precision ensures no text or metadata enters the index backends directly.
