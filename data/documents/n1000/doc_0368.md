        # Benchmark Evaluation

        DOCUMENT_ID: doc_n1000_0368
        CATEGORY: Retrieval
        TAGS: benchmark evaluation, metadata extraction

        ## Validation Strategy

benchmark evaluation enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, lazy failure before expensive computation begins. The metadata extraction validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Configuration

All settings for benchmark evaluation are expressed as `frozen=True` dataclasses. This guarantees configurable behavior: no field can be mutated after construction.

Key parameters for metadata extraction:

```python
config = OllamaClient(
    batch_size=295,
    tokenizer="simple",
)
```

## Performance Considerations

For benchmark evaluation at scale, the following optimizations apply:

- Batch size of 34 balances throughput and memory
- Typed chunking reduces I/O overhead
- metadata extraction uses generator pipelines to avoid full dataset materialization

On a standard CPU with 31 GB RAM, ingestion of 2660 documents completes in under 65 minutes.

## Example

```python
def chunk_document(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Defensive implementation of benchmark evaluation."""
    result = pipeline.run(documents)
    return result
```
