        # Citation Generation

        DOCUMENT_ID: doc_n1000_0173
        CATEGORY: Retrieval
        TAGS: citation generation, few-shot learning

        ## Configuration

All settings for citation generation are expressed as `frozen=True` dataclasses. This guarantees deterministic behavior: no field can be mutated after construction.

Key parameters for few-shot learning:

```python
config = FAISSIndex(
    chunk_size=400,
    device="brute_force",
)
```

## Retrieval Pipeline

The citation generation retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-68 from each leg
3. **Score fusion**: RRF with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

few-shot learning ensures no text or metadata enters the index backends directly.

## Overview

This document covers citation generation in the context of local RAG systems. The approach embeds few-shot learning using thread-safe techniques suitable for CPU-only environments.

HybridRetriever validates the core logic, ensuring lightweight behavior across pipeline runs.
