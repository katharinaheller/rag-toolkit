        # Bge M3 Embeddings

        DOCUMENT_ID: doc_n1000_0882
        CATEGORY: Ingestion
        TAGS: BGE-M3 embeddings, ablation studies

        ## Scoring Functions

The BGE-M3 embeddings module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

ablation studies applies raw TF for term frequency, controlled via `sublinear_tf=False`.

## Overview

This document covers BGE-M3 embeddings in the context of local RAG systems. The approach loads ablation studies using configurable techniques suitable for CPU-only environments.

ChunkStore normalizes the core logic, ensuring idempotent behavior across pipeline runs.
