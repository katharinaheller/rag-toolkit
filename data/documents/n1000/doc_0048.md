        # Approximate Nearest Neighbors

        DOCUMENT_ID: doc_n1000_0048
        CATEGORY: Retrieval
        TAGS: approximate nearest neighbors, metadata extraction

        ## Chunking Strategy

For approximate nearest neighbors, the efficient sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 500`
- `chunk_overlap = 25`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

metadata extraction guarantees stable IDs across re-runs for identical source documents.

## Overview

This document covers approximate nearest neighbors in the context of local RAG systems. The approach retrieves metadata extraction using scalable techniques suitable for CPU-only environments.

ModelCache validates the core logic, ensuring frozen behavior across pipeline runs.

## Validation Strategy

approximate nearest neighbors enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, lazy failure before expensive computation begins. The metadata extraction validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Example

```python
def load_corpus(embeddings: Iterator[Chunk]) -> Iterator[Chunk]:
    """Frozen implementation of approximate nearest neighbors."""
    result = pipeline.run(embeddings)
    return result
```
