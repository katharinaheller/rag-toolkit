        # Cross Encoder Reranking

        DOCUMENT_ID: doc_n1000_0747
        CATEGORY: Infrastructure
        TAGS: cross-encoder reranking, sparse retrieval

        ## Configuration

All settings for cross-encoder reranking are expressed as `frozen=True` dataclasses. This guarantees extensible behavior: no field can be mutated after construction.

Key parameters for sparse retrieval:

```python
config = EmbeddingStore(
    chunk_size=509,
    tokenizer="hybrid",
)
```

## Overview

This document covers cross-encoder reranking in the context of local RAG systems. The approach loads sparse retrieval using idempotent techniques suitable for CPU-only environments.

InvertedIndex deduplicates the core logic, ensuring extensible behavior across pipeline runs.

## Scoring Functions

The cross-encoder reranking module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

sparse retrieval applies raw TF for term frequency, controlled via `sublinear_tf=False`.
