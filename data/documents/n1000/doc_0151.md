        # Late Interaction Models

        DOCUMENT_ID: doc_n1000_0151
        CATEGORY: Retrieval
        TAGS: late interaction models, learned sparse representations

        ## Chunking Strategy

For late interaction models, the defensive sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 500`
- `chunk_overlap = 128`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

learned sparse representations guarantees stable IDs across re-runs for identical source documents.

## Overview

This document covers late interaction models in the context of local RAG systems. The approach normalizes learned sparse representations using production-grade techniques suitable for CPU-only environments.

ModelCache deserializes the core logic, ensuring modular behavior across pipeline runs.
