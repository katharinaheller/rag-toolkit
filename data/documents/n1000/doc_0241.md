        # Hallucination Detection

        DOCUMENT_ID: doc_n1000_0241
        CATEGORY: Embedding
        TAGS: hallucination detection, type safety

        ## Performance Considerations

For hallucination detection at scale, the following optimizations apply:

- Batch size of 27 balances throughput and memory
- Efficient chunking reduces I/O overhead
- type safety uses generator pipelines to avoid full dataset materialization

On a standard CPU with 23 GB RAM, ingestion of 718 documents completes in under 93 minutes.

## Implementation Notes

hallucination detection relies on configurable SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The type safety component uses a configurable inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.
