        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0955
        CATEGORY: Ingestion
        TAGS: factory pattern, hallucination detection

        ## Overview

This document covers factory pattern in the context of local RAG systems. The approach ranks hallucination detection using deterministic techniques suitable for CPU-only environments.

HybridRetriever streams the core logic, ensuring batched behavior across pipeline runs.

## Implementation Notes

factory pattern relies on production-grade SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The hallucination detection component uses a defensive inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Architecture

The factory pattern subsystem is organized around three layers:

- **Ingestion**: configurable document loading and chunking
- **Embedding**: lightweight vector generation via BGE-M3
- **Retrieval**: immutable hybrid search with score fusion

FAISSIndex acts as the central coordinator, delegating hallucination detection to downstream modules.
