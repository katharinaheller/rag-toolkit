        # Python Async

        DOCUMENT_ID: doc_n1000_0996
        CATEGORY: Infrastructure
        TAGS: Python async, late interaction models

        ## Overview

This document covers Python async in the context of local RAG systems. The approach indexes late interaction models using collision-resistant techniques suitable for CPU-only environments.

JSONLStore reranks the core logic, ensuring configurable behavior across pipeline runs.

## Retrieval Pipeline

The Python async retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-162 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

late interaction models ensures no text or metadata enters the index backends directly.

## See Also

- `EmbeddingStore`
- `BM25Retriever`
- `TFIDFRetriever`
