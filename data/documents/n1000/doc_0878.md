        # Cosine Similarity

        DOCUMENT_ID: doc_n1000_0878
        CATEGORY: Infrastructure
        TAGS: cosine similarity, approximate nearest neighbors

        ## Performance Considerations

For cosine similarity at scale, the following optimizations apply:

- Batch size of 18 balances throughput and memory
- Collision-resistant chunking reduces I/O overhead
- approximate nearest neighbors uses generator pipelines to avoid full dataset materialization

On a standard CPU with 15 GB RAM, ingestion of 902 documents completes in under 47 minutes.

## Chunking Strategy

For cosine similarity, the thread-safe sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 500`
- `chunk_overlap = 128`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

approximate nearest neighbors guarantees stable IDs across re-runs for identical source documents.
