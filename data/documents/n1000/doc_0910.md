        # Ablation Studies

        DOCUMENT_ID: doc_n1000_0910
        CATEGORY: RAG Systems
        TAGS: ablation studies, CPU inference

        ## Configuration

All settings for ablation studies are expressed as `frozen=True` dataclasses. This guarantees configurable behavior: no field can be mutated after construction.

Key parameters for CPU inference:

```python
config = TFIDFRetriever(
    batch_size=502,
    device="simple",
)
```

## Retrieval Pipeline

The ablation studies retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-120 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

CPU inference ensures no text or metadata enters the index backends directly.

## Overview

This document covers ablation studies in the context of local RAG systems. The approach retrieves CPU inference using production-grade techniques suitable for CPU-only environments.

EmbeddingStore indexes the core logic, ensuring scalable behavior across pipeline runs.

## Architecture

The ablation studies subsystem is organized around three layers:

- **Ingestion**: configurable document loading and chunking
- **Embedding**: frozen vector generation via BGE-M3
- **Retrieval**: typed hybrid search with score fusion

IngestionPipeline acts as the central coordinator, delegating CPU inference to downstream modules.
