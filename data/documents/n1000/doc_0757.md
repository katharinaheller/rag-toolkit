        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0757
        CATEGORY: Evaluation
        TAGS: factory pattern, pipeline orchestration

        ## Retrieval Pipeline

The factory pattern retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-191 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

pipeline orchestration ensures no text or metadata enters the index backends directly.

## Performance Considerations

For factory pattern at scale, the following optimizations apply:

- Batch size of 21 balances throughput and memory
- Efficient chunking reduces I/O overhead
- pipeline orchestration uses generator pipelines to avoid full dataset materialization

On a standard CPU with 18 GB RAM, ingestion of 1489 documents completes in under 104 minutes.
