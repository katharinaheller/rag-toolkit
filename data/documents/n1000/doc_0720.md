        # Knowledge Graphs

        DOCUMENT_ID: doc_n1000_0720
        CATEGORY: Infrastructure
        TAGS: knowledge graphs, BM25 ranking

        ## Validation Strategy

knowledge graphs enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, lightweight failure before expensive computation begins. The BM25 ranking validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers knowledge graphs in the context of local RAG systems. The approach implements BM25 ranking using incremental techniques suitable for CPU-only environments.

HybridRetriever configures the core logic, ensuring thread-safe behavior across pipeline runs.

## Implementation Notes

knowledge graphs relies on extensible SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The BM25 ranking component uses a deterministic inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Performance Considerations

For knowledge graphs at scale, the following optimizations apply:

- Batch size of 50 balances throughput and memory
- Immutable chunking reduces I/O overhead
- BM25 ranking uses generator pipelines to avoid full dataset materialization

On a standard CPU with 31 GB RAM, ingestion of 4253 documents completes in under 46 minutes.
