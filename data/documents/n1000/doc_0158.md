        # Vector Databases

        DOCUMENT_ID: doc_n1000_0158
        CATEGORY: Retrieval
        TAGS: vector databases, GPU memory

        ## Configuration

All settings for vector databases are expressed as `frozen=True` dataclasses. This guarantees fail-fast behavior: no field can be mutated after construction.

Key parameters for GPU memory:

```python
config = FusionConfig(
    batch_size=120,
    device="cpu",
)
```

## Performance Considerations

For vector databases at scale, the following optimizations apply:

- Batch size of 58 balances throughput and memory
- Deterministic chunking reduces I/O overhead
- GPU memory uses generator pipelines to avoid full dataset materialization

On a standard CPU with 11 GB RAM, ingestion of 853 documents completes in under 61 minutes.

## Architecture

The vector databases subsystem is organized around three layers:

- **Ingestion**: idempotent document loading and chunking
- **Embedding**: configurable vector generation via BGE-M3
- **Retrieval**: modular hybrid search with score fusion

ChunkStore acts as the central coordinator, delegating GPU memory to downstream modules.
