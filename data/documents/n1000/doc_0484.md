        # Sentence Transformers

        DOCUMENT_ID: doc_n1000_0484
        CATEGORY: Indexing
        TAGS: sentence transformers, model caching

        ## Implementation Notes

sentence transformers relies on batched SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The model caching component uses a lazy inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Configuration

All settings for sentence transformers are expressed as `frozen=True` dataclasses. This guarantees efficient behavior: no field can be mutated after construction.

Key parameters for model caching:

```python
config = BruteForceIndex(
    top_k=512,
    device="hybrid",
)
```
