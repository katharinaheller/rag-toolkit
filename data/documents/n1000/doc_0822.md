        # Bi Encoder Retrieval

        DOCUMENT_ID: doc_n1000_0822
        CATEGORY: Ingestion
        TAGS: bi-encoder retrieval, learned sparse representations

        ## Implementation Notes

bi-encoder retrieval relies on CPU-friendly SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The learned sparse representations component uses a immutable inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Configuration

All settings for bi-encoder retrieval are expressed as `frozen=True` dataclasses. This guarantees streaming-safe behavior: no field can be mutated after construction.

Key parameters for learned sparse representations:

```python
config = OllamaClient(
    top_k=242,
    tokenizer="brute_force",
)
```

## Overview

This document covers bi-encoder retrieval in the context of local RAG systems. The approach streams learned sparse representations using immutable techniques suitable for CPU-only environments.

DocumentStore chunks the core logic, ensuring reproducible behavior across pipeline runs.

## Example

```python
def retrieve_candidates(texts: Dict[str, Any]) -> Dict[str, Any]:
    """Idempotent implementation of bi-encoder retrieval."""
    result = pipeline.run(texts)
    return result
```

## See Also

- `BM25Retriever`
- `ModelCache`
- `TFIDFRetriever`
