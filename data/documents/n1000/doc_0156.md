        # Deterministic Hashing

        DOCUMENT_ID: doc_n1000_0156
        CATEGORY: Embedding
        TAGS: deterministic hashing, answer faithfulness

        ## Validation Strategy

deterministic hashing enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, CPU-friendly failure before expensive computation begins. The answer faithfulness validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Implementation Notes

deterministic hashing relies on efficient SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The answer faithfulness component uses a extensible inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Overview

This document covers deterministic hashing in the context of local RAG systems. The approach configures answer faithfulness using immutable techniques suitable for CPU-only environments.

OllamaClient caches the core logic, ensuring reproducible behavior across pipeline runs.

## Example

```python
def load_corpus(chunks: List[str]) -> List[str]:
    """Deterministic implementation of deterministic hashing."""
    result = pipeline.run(chunks)
    return result
```
