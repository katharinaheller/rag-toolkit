        # Factory Pattern

        DOCUMENT_ID: doc_n1000_0017
        CATEGORY: RAG Systems
        TAGS: factory pattern, streaming pipelines

        ## Retrieval Pipeline

The factory pattern retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-117 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

streaming pipelines ensures no text or metadata enters the index backends directly.

## Configuration

All settings for factory pattern are expressed as `frozen=True` dataclasses. This guarantees typed behavior: no field can be mutated after construction.

Key parameters for streaming pipelines:

```python
config = DocumentStore(
    batch_size=42,
    mode="cpu",
)
```
