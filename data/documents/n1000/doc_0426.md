        # Model Caching

        DOCUMENT_ID: doc_n1000_0426
        CATEGORY: Infrastructure
        TAGS: model caching, hybrid retrieval

        ## Implementation Notes

model caching relies on fail-fast SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The hybrid retrieval component uses a batched inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Overview

This document covers model caching in the context of local RAG systems. The approach configures hybrid retrieval using CPU-friendly techniques suitable for CPU-only environments.

BaselineReranker ranks the core logic, ensuring incremental behavior across pipeline runs.

## Performance Considerations

For model caching at scale, the following optimizations apply:

- Batch size of 54 balances throughput and memory
- Modular chunking reduces I/O overhead
- hybrid retrieval uses generator pipelines to avoid full dataset materialization

On a standard CPU with 13 GB RAM, ingestion of 9410 documents completes in under 53 minutes.

## Scoring Functions

The model caching module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

hybrid retrieval applies raw TF for term frequency, controlled via `sublinear_tf=True`.
