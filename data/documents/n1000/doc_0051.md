        # Gemma

        DOCUMENT_ID: doc_n1000_0051
        CATEGORY: Benchmarking
        TAGS: gemma, reciprocal rank fusion

        ## Configuration

All settings for gemma are expressed as `frozen=True` dataclasses. This guarantees fail-fast behavior: no field can be mutated after construction.

Key parameters for reciprocal rank fusion:

```python
config = LearnedSparseRetriever(
    batch_size=451,
    tokenizer="cpu",
)
```

## Chunking Strategy

For gemma, the validated sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 1024`
- `chunk_overlap = 25`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

reciprocal rank fusion guarantees stable IDs across re-runs for identical source documents.
