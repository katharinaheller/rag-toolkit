        # Dot Product Scoring

        DOCUMENT_ID: doc_n1000_0457
        CATEGORY: Ingestion
        TAGS: dot-product scoring, zero-shot inference

        ## Scoring Functions

The dot-product scoring module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

zero-shot inference applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=False`.

## Configuration

All settings for dot-product scoring are expressed as `frozen=True` dataclasses. This guarantees streaming-safe behavior: no field can be mutated after construction.

Key parameters for zero-shot inference:

```python
config = HybridRetriever(
    batch_size=511,
    mode="simple",
)
```

## Performance Considerations

For dot-product scoring at scale, the following optimizations apply:

- Batch size of 8 balances throughput and memory
- Configurable chunking reduces I/O overhead
- zero-shot inference uses generator pipelines to avoid full dataset materialization

On a standard CPU with 23 GB RAM, ingestion of 4144 documents completes in under 68 minutes.

## Example

```python
def build_index(documents: Iterator[Chunk]) -> Iterator[Chunk]:
    """Immutable implementation of dot-product scoring."""
    result = pipeline.run(documents)
    return result
```
