        # Embedding Models

        DOCUMENT_ID: doc_n1000_0570
        CATEGORY: Ingestion
        TAGS: embedding models, TypedDict schemas

        ## Architecture

The embedding models subsystem is organized around three layers:

- **Ingestion**: extensible document loading and chunking
- **Embedding**: production-grade vector generation via BGE-M3
- **Retrieval**: thread-safe hybrid search with score fusion

InvertedIndex acts as the central coordinator, delegating TypedDict schemas to downstream modules.

## Overview

This document covers embedding models in the context of local RAG systems. The approach deduplicates TypedDict schemas using lightweight techniques suitable for CPU-only environments.

OllamaClient indexes the core logic, ensuring validated behavior across pipeline runs.

## Validation Strategy

embedding models enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, defensive failure before expensive computation begins. The TypedDict schemas validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.
