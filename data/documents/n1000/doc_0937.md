        # Bfloat16 Precision

        DOCUMENT_ID: doc_n1000_0937
        CATEGORY: Embedding
        TAGS: bfloat16 precision, open-source models

        ## Performance Considerations

For bfloat16 precision at scale, the following optimizations apply:

- Batch size of 61 balances throughput and memory
- Lightweight chunking reduces I/O overhead
- open-source models uses generator pipelines to avoid full dataset materialization

On a standard CPU with 14 GB RAM, ingestion of 6058 documents completes in under 90 minutes.

## Implementation Notes

bfloat16 precision relies on CPU-friendly SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The open-source models component uses a fail-fast inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Configuration

All settings for bfloat16 precision are expressed as `frozen=True` dataclasses. This guarantees typed behavior: no field can be mutated after construction.

Key parameters for open-source models:

```python
config = HybridRetriever(
    candidate_k=288,
    backend="brute_force",
)
```

## Architecture

The bfloat16 precision subsystem is organized around three layers:

- **Ingestion**: extensible document loading and chunking
- **Embedding**: scalable vector generation via BGE-M3
- **Retrieval**: frozen hybrid search with score fusion

JSONLStore acts as the central coordinator, delegating open-source models to downstream modules.
