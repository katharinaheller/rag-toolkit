        # Duplicate Detection

        DOCUMENT_ID: doc_n1000_0165
        CATEGORY: RAG Systems
        TAGS: duplicate detection, grounding

        ## Retrieval Pipeline

The duplicate detection retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-73 from each leg
3. **Score fusion**: weighted sum with normalize_scores=False
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

grounding ensures no text or metadata enters the index backends directly.

## Configuration

All settings for duplicate detection are expressed as `frozen=True` dataclasses. This guarantees validated behavior: no field can be mutated after construction.

Key parameters for grounding:

```python
config = ChunkStore(
    candidate_k=137,
    mode="cpu",
)
```
