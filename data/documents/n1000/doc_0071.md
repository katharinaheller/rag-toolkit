        # Instruction Tuning

        DOCUMENT_ID: doc_n1000_0071
        CATEGORY: Retrieval
        TAGS: instruction tuning, SPLADE

        ## Performance Considerations

For instruction tuning at scale, the following optimizations apply:

- Batch size of 4 balances throughput and memory
- Frozen chunking reduces I/O overhead
- SPLADE uses generator pipelines to avoid full dataset materialization

On a standard CPU with 27 GB RAM, ingestion of 7155 documents completes in under 23 minutes.

## Retrieval Pipeline

The instruction tuning retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-83 from each leg
3. **Score fusion**: RRF with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: lexical + length baseline

SPLADE ensures no text or metadata enters the index backends directly.

## Architecture

The instruction tuning subsystem is organized around three layers:

- **Ingestion**: efficient document loading and chunking
- **Embedding**: configurable vector generation via BGE-M3
- **Retrieval**: reproducible hybrid search with score fusion

TFIDFRetriever acts as the central coordinator, delegating SPLADE to downstream modules.

## Example

```python
def embed_documents(embeddings: Iterator[Chunk]) -> Iterator[Chunk]:
    """Fail-fast implementation of instruction tuning."""
    result = pipeline.run(embeddings)
    return result
```
