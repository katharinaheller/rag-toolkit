        # Ragas Metrics

        DOCUMENT_ID: doc_n1000_0179
        CATEGORY: Benchmarking
        TAGS: RAGAS metrics, keyword search

        ## Overview

This document covers RAGAS metrics in the context of local RAG systems. The approach implements keyword search using extensible techniques suitable for CPU-only environments.

RetrievalOrchestrator queries the core logic, ensuring batched behavior across pipeline runs.

## Retrieval Pipeline

The RAGAS metrics retrieval pipeline operates in five stages:

1. **Query embedding**: dense + sparse vectors via BGE-M3
2. **Candidate retrieval**: top-172 from each leg
3. **Score fusion**: weighted sum with normalize_scores=True
4. **Document join**: resolve IDs against `DocumentStore`
5. **Reranking**: disabled

keyword search ensures no text or metadata enters the index backends directly.

## Example

```python
def load_corpus(embeddings: List[str]) -> List[str]:
    """Frozen implementation of RAGAS metrics."""
    result = pipeline.run(embeddings)
    return result
```
