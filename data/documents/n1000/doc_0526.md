        # Float16 Precision

        DOCUMENT_ID: doc_n1000_0526
        CATEGORY: Retrieval
        TAGS: float16 precision, hallucination detection

        ## Validation Strategy

float16 precision enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, thread-safe failure before expensive computation begins. The hallucination detection validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Configuration

All settings for float16 precision are expressed as `frozen=True` dataclasses. This guarantees idempotent behavior: no field can be mutated after construction.

Key parameters for hallucination detection:

```python
config = BruteForceIndex(
    candidate_k=473,
    tokenizer="brute_force",
)
```

## Performance Considerations

For float16 precision at scale, the following optimizations apply:

- Batch size of 63 balances throughput and memory
- Streaming-safe chunking reduces I/O overhead
- hallucination detection uses generator pipelines to avoid full dataset materialization

On a standard CPU with 29 GB RAM, ingestion of 1351 documents completes in under 101 minutes.

## Scoring Functions

The float16 precision module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

hallucination detection applies sublinear TF scaling for term frequency, controlled via `sublinear_tf=True`.

## See Also

- `BM25Retriever`
- `LearnedSparseRetriever`
