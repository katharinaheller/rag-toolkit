        # Colbert

        DOCUMENT_ID: doc_n1000_0897
        CATEGORY: Benchmarking
        TAGS: ColBERT, hallucination detection

        ## Chunking Strategy

For ColBERT, the modular sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 512`
- `chunk_overlap = 128`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

hallucination detection guarantees stable IDs across re-runs for identical source documents.

## Performance Considerations

For ColBERT at scale, the following optimizations apply:

- Batch size of 44 balances throughput and memory
- Fail-fast chunking reduces I/O overhead
- hallucination detection uses generator pipelines to avoid full dataset materialization

On a standard CPU with 11 GB RAM, ingestion of 2507 documents completes in under 43 minutes.

## See Also

- `SparseIndex`
- `PromptBuilder`
- `OllamaClient`
- `FusionConfig`
