        # Cross Encoder Reranking

        DOCUMENT_ID: doc_n1000_0317
        CATEGORY: Embedding
        TAGS: cross-encoder reranking, open-source models

        ## Chunking Strategy

For cross-encoder reranking, the typed sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 256`
- `chunk_overlap = 128`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

open-source models guarantees stable IDs across re-runs for identical source documents.

## Performance Considerations

For cross-encoder reranking at scale, the following optimizations apply:

- Batch size of 33 balances throughput and memory
- Validated chunking reduces I/O overhead
- open-source models uses generator pipelines to avoid full dataset materialization

On a standard CPU with 32 GB RAM, ingestion of 7787 documents completes in under 74 minutes.

## Overview

This document covers cross-encoder reranking in the context of local RAG systems. The approach deserializes open-source models using collision-resistant techniques suitable for CPU-only environments.

TFIDFRetriever normalizes the core logic, ensuring thread-safe behavior across pipeline runs.

## Configuration

All settings for cross-encoder reranking are expressed as `frozen=True` dataclasses. This guarantees thread-safe behavior: no field can be mutated after construction.

Key parameters for open-source models:

```python
config = SparseIndex(
    batch_size=359,
    backend="brute_force",
)
```

## Example

```python
def validate_ids(query: List[float]) -> List[float]:
    """Fail-fast implementation of cross-encoder reranking."""
    result = pipeline.run(query)
    return result
```

## See Also

- `FusionConfig`
- `JSONLStore`
- `OllamaClient`
