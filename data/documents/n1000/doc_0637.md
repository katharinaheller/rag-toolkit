        # Chunk Store

        DOCUMENT_ID: doc_n1000_0637
        CATEGORY: Retrieval
        TAGS: chunk store, RAG evaluation

        ## Performance Considerations

For chunk store at scale, the following optimizations apply:

- Batch size of 47 balances throughput and memory
- Production-grade chunking reduces I/O overhead
- RAG evaluation uses generator pipelines to avoid full dataset materialization

On a standard CPU with 29 GB RAM, ingestion of 6182 documents completes in under 67 minutes.

## Chunking Strategy

For chunk store, the defensive sliding-window chunker splits documents into overlapping character windows.

Default settings:
- `chunk_size = 512`
- `chunk_overlap = 128`
- `strategy = FIXED_OVERLAP`

Chunk IDs are derived as:
```
chunk_id = SHA-256({document_id}:{strategy}:{index})[:16]
```

RAG evaluation guarantees stable IDs across re-runs for identical source documents.
