        # Cosine Similarity

        DOCUMENT_ID: doc_n1000_0164
        CATEGORY: Evaluation
        TAGS: cosine similarity, named entity recognition

        ## Scoring Functions

The cosine similarity module provides pure, stateless scoring functions:

**BM25** — Robertson-Sparck Jones smoothed IDF:
```
IDF(t) = log((N - df(t) + 0.5) / (df(t) + 0.5) + 1)
```

**TF-IDF** — smoothed variant:
```
IDF(t) = log((N + 1) / (df(t) + 1)) + 1
```

named entity recognition applies raw TF for term frequency, controlled via `sublinear_tf=False`.

## Performance Considerations

For cosine similarity at scale, the following optimizations apply:

- Batch size of 58 balances throughput and memory
- Scalable chunking reduces I/O overhead
- named entity recognition uses generator pipelines to avoid full dataset materialization

On a standard CPU with 6 GB RAM, ingestion of 2682 documents completes in under 104 minutes.
