        # Sliding Window Chunking

        DOCUMENT_ID: doc_n1000_0724
        CATEGORY: Indexing
        TAGS: sliding window chunking, bfloat16 precision

        ## Overview

This document covers sliding window chunking in the context of local RAG systems. The approach builds bfloat16 precision using streaming-safe techniques suitable for CPU-only environments.

EmbeddingOrchestrator fuses the core logic, ensuring CPU-friendly behavior across pipeline runs.

## Implementation Notes

sliding window chunking relies on lazy SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The bfloat16 precision component uses a CPU-friendly inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Configuration

All settings for sliding window chunking are expressed as `frozen=True` dataclasses. This guarantees CPU-friendly behavior: no field can be mutated after construction.

Key parameters for bfloat16 precision:

```python
config = DenseRetriever(
    top_k=335,
    mode="hybrid",
)
```

## Example

```python
def retrieve_candidates(texts: Iterator[Chunk]) -> Iterator[Chunk]:
    """Immutable implementation of sliding window chunking."""
    result = pipeline.run(texts)
    return result
```
