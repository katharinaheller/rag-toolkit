        # Hybrid Retrieval

        DOCUMENT_ID: doc_n1000_0419
        CATEGORY: Retrieval
        TAGS: hybrid retrieval, inverted index

        ## Validation Strategy

hybrid retrieval enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, production-grade failure before expensive computation begins. The inverted index validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Overview

This document covers hybrid retrieval in the context of local RAG systems. The approach persists inverted index using frozen techniques suitable for CPU-only environments.

EmbeddingOrchestrator configures the core logic, ensuring typed behavior across pipeline runs.

## Performance Considerations

For hybrid retrieval at scale, the following optimizations apply:

- Batch size of 26 balances throughput and memory
- Efficient chunking reduces I/O overhead
- inverted index uses generator pipelines to avoid full dataset materialization

On a standard CPU with 31 GB RAM, ingestion of 832 documents completes in under 54 minutes.

## Configuration

All settings for hybrid retrieval are expressed as `frozen=True` dataclasses. This guarantees frozen behavior: no field can be mutated after construction.

Key parameters for inverted index:

```python
config = LoaderRegistry(
    candidate_k=293,
    tokenizer="brute_force",
)
```
