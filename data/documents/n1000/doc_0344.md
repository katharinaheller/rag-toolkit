        # Streaming Pipelines

        DOCUMENT_ID: doc_n1000_0344
        CATEGORY: Retrieval
        TAGS: streaming pipelines, sentence transformers

        ## Configuration

All settings for streaming pipelines are expressed as `frozen=True` dataclasses. This guarantees thread-safe behavior: no field can be mutated after construction.

Key parameters for sentence transformers:

```python
config = ContextPreparer(
    chunk_size=246,
    backend="simple",
)
```

## Implementation Notes

streaming pipelines relies on immutable SHA-256 prefix hashing for identifier generation. Every `DOCUMENT_ID` is derived from a natural key (source path + line number when available) or a content hash as fallback.

The sentence transformers component uses a incremental inverted index that stores pre-tokenized term frequencies. Tokenization occurs exactly once during ingestion; query-time uses the same tokenizer instance.

## Overview

This document covers streaming pipelines in the context of local RAG systems. The approach implements sentence transformers using thread-safe techniques suitable for CPU-only environments.

InvertedIndex wraps the core logic, ensuring deterministic behavior across pipeline runs.

## See Also

- `ModelCache`
- `HybridRetriever`
- `TFIDFRetriever`
- `PromptBuilder`
