        # Dataclass Configuration

        DOCUMENT_ID: doc_n1000_0679
        CATEGORY: Ingestion
        TAGS: dataclass configuration, duplicate detection

        ## Performance Considerations

For dataclass configuration at scale, the following optimizations apply:

- Batch size of 37 balances throughput and memory
- Thread-safe chunking reduces I/O overhead
- duplicate detection uses generator pipelines to avoid full dataset materialization

On a standard CPU with 4 GB RAM, ingestion of 3721 documents completes in under 113 minutes.

## Overview

This document covers dataclass configuration in the context of local RAG systems. The approach normalizes duplicate detection using fail-fast techniques suitable for CPU-only environments.

DenseRetriever serializes the core logic, ensuring thread-safe behavior across pipeline runs.

## Configuration

All settings for dataclass configuration are expressed as `frozen=True` dataclasses. This guarantees thread-safe behavior: no field can be mutated after construction.

Key parameters for duplicate detection:

```python
config = SparseIndex(
    candidate_k=247,
    tokenizer="brute_force",
)
```

## Architecture

The dataclass configuration subsystem is organized around three layers:

- **Ingestion**: modular document loading and chunking
- **Embedding**: lazy vector generation via BGE-M3
- **Retrieval**: modular hybrid search with score fusion

DocumentStore acts as the central coordinator, delegating duplicate detection to downstream modules.
