        # Open Source Models

        DOCUMENT_ID: doc_n1000_0293
        CATEGORY: Retrieval
        TAGS: open-source models, reciprocal rank fusion

        ## Configuration

All settings for open-source models are expressed as `frozen=True` dataclasses. This guarantees extensible behavior: no field can be mutated after construction.

Key parameters for reciprocal rank fusion:

```python
config = TFIDFRetriever(
    candidate_k=377,
    tokenizer="brute_force",
)
```

## Overview

This document covers open-source models in the context of local RAG systems. The approach loads reciprocal rank fusion using lightweight techniques suitable for CPU-only environments.

IngestionPipeline validates the core logic, ensuring production-grade behavior across pipeline runs.

## Example

```python
def fuse_results(embeddings: List[str]) -> List[str]:
    """Immutable implementation of open-source models."""
    result = pipeline.run(embeddings)
    return result
```
