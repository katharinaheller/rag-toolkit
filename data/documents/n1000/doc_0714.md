        # Citation Generation

        DOCUMENT_ID: doc_n1000_0714
        CATEGORY: Embedding
        TAGS: citation generation, hallucination detection

        ## Validation Strategy

citation generation enforces a three-layer uniqueness contract:

```
DOCUMENT_ID → chunk_id → embedding_vector_id
```

Any collision triggers an immediate, modular failure before expensive computation begins. The hallucination detection validator reports every duplicate with its source file path for fast diagnosis.

Policy: **no auto-deduplication**, **no silent skipping**.

## Configuration

All settings for citation generation are expressed as `frozen=True` dataclasses. This guarantees frozen behavior: no field can be mutated after construction.

Key parameters for hallucination detection:

```python
config = SparseIndex(
    chunk_size=204,
    mode="simple",
)
```

## Example

```python
def retrieve_candidates(chunks: Dict[str, Any]) -> Dict[str, Any]:
    """Efficient implementation of citation generation."""
    result = pipeline.run(chunks)
    return result
```

## See Also

- `DocumentStore`
- `BM25Retriever`
- `IngestionPipeline`
